---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="3 GaLaBau-Aufträge in 30 Tagen — Garantiert | Fastra One"
  description="Wir garantieren dir 3 qualifizierte Auftragsanfragen in 30 Tagen — oder du zahlst nichts. Exklusiv für GaLaBau-Betriebe."
  navVariant="none"
  footerVariant="none"
>

<Fragment slot="head">
  <meta name="robots" content="noindex, nofollow" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@1,9..144,500&display=swap" rel="stylesheet" />
  <style is:global>

/* ═══════════════════════════════════
   TOKENS
═══════════════════════════════════ */
:root {
  --accent:       #22c55e;
  --accent-deep:  #15803d;
  --accent-soft:  rgba(34,197,94,.09);
  --accent-line:  rgba(34,197,94,.22);
  --ink:          #0d1117;
  --ink-2:        #374151;
  --bg:           #ffffff;
  --bg-alt:       #f8fdf9;
  --line:         rgba(0,0,0,.06);
  --card-line:    #e5e7eb;
  --red:          #ef4444;
  --red-soft:     rgba(239,68,68,.07);
  --red-line:     rgba(239,68,68,.22);
  --f-serif:      'Fraunces', Georgia, serif;
  --f-sans:       'Inter', -apple-system, sans-serif;
  --r-pill:       100px;
  --r-card:       18px;
  --r-sm:         10px;
  --shadow-sm:    0 1px 2px rgba(0,0,0,.05), 0 4px 12px rgba(0,0,0,.06);
  --shadow-md:    0 2px 4px rgba(0,0,0,.04), 0 12px 32px rgba(0,0,0,.08);
  --shadow-lg:    0 4px 8px rgba(0,0,0,.04), 0 24px 64px rgba(0,0,0,.10);
  --shadow-xl:    0 8px 16px rgba(0,0,0,.04), 0 40px 80px -12px rgba(0,0,0,.16);
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html  { scroll-behavior: smooth; }
body  {
  font-family: var(--f-sans); background: var(--bg); color: var(--ink);
  -webkit-font-smoothing: antialiased; overflow-x: hidden;
  font-feature-settings: 'rlig' 1, 'calt' 1;
}
a     { color: inherit; text-decoration: none; }
img   { max-width: 100%; display: block; }
:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; }
.wrap { max-width: 1100px; margin: 0 auto; padding: 0 28px; }

/* ═══════════════════════════════════
   REVEAL ANIMATIONS
═══════════════════════════════════ */
.reveal { opacity: 0; transform: translateY(20px); transition: opacity .65s ease, transform .65s ease; }
.reveal.is-visible { opacity: 1; transform: none; }
.reveal.d1 { transition-delay: .1s; }
.reveal.d2 { transition-delay: .2s; }
.reveal.d3 { transition-delay: .3s; }
@media (prefers-reduced-motion: reduce) { .reveal { opacity: 1; transform: none; transition: none; } }

/* ═══════════════════════════════════
   URGENCY BAR
═══════════════════════════════════ */
.urg {
  background: var(--red);
  color: #fff;
  text-align: center;
  padding: 9px 20px;
  font-size: .8rem;
  font-weight: 700;
  letter-spacing: .02em;
}

/* ═══════════════════════════════════
   NAV — Pill (Faris-Schmidt-Referenz)
═══════════════════════════════════ */
.nav-outer {
  position: sticky; top: 0; z-index: 100;
  display: flex; justify-content: center;
  padding: 12px 16px;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}
nav.pill {
  width: 100%; max-width: 1060px;
  display: flex; align-items: center; justify-content: space-between;
  background: rgba(255,255,255,.9);
  border: 1px solid rgba(0,0,0,.06);
  border-radius: var(--r-pill);
  padding: 10px 10px 10px 22px;
  box-shadow: 0 1px 3px rgba(0,0,0,.04), 0 8px 24px rgba(0,0,0,.05);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}
.nav-brand {
  display: flex; align-items: center; gap: 8px;
  font-weight: 800; font-size: .98rem; letter-spacing: -.02em;
}
.nav-dot { width: 10px; height: 10px; border-radius: 50%; background: var(--accent); }
nav.pill ul { display: flex; gap: 26px; list-style: none; }
nav.pill ul a { font-size: .86rem; font-weight: 500; color: var(--ink-2); transition: color .2s; }
nav.pill ul a:hover { color: var(--ink); }
@media (max-width: 700px) { nav.pill ul { display: none; } }

/* ═══════════════════════════════════
   BUTTONS
═══════════════════════════════════ */
.btn {
  display: inline-flex; align-items: center; gap: 8px;
  border-radius: var(--r-pill); border: none; cursor: pointer;
  font-family: var(--f-sans); font-weight: 400; font-size: .92rem;
  letter-spacing: -.02em; white-space: nowrap;
  padding: 11px 11px 11px 20px;
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset;
  transition: transform .3s cubic-bezier(.34,1.56,.64,1), box-shadow .3s;
}
.btn:hover { transform: translateY(-2px); }
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover { box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset, rgba(34,197,94,.30) 0 8px 28px; }
.btn-dark { background: var(--ink); color: #fff; }
.btn-dark:hover { box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset, rgba(0,0,0,.22) 0 8px 28px; }
.btn-outline {
  background: var(--bg); color: var(--ink);
  border: 1px solid var(--card-line); box-shadow: none;
  padding: 11px 20px;
}
.btn-outline:hover { box-shadow: var(--shadow-sm); }
.btn-sm { font-size: .8rem; padding: 8px 8px 8px 14px; }
.btn-arrow {
  width: 26px; height: 26px; border-radius: 50%; background: #fff;
  display: inline-flex; align-items: center; justify-content: center; flex: none;
}
.btn-primary .btn-arrow { color: var(--accent); }
.btn-dark    .btn-arrow { color: var(--ink); }
.btn-sm .btn-arrow { width: 22px; height: 22px; }

/* ═══════════════════════════════════
   PULSE DOT
═══════════════════════════════════ */
.pulse-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--accent); position: relative; flex: none; }
.pulse-dot::after {
  content: ''; position: absolute; inset: -5px;
  border-radius: 50%; border: 2px solid var(--accent);
  animation: pulse-ring 2s ease infinite;
}
@keyframes pulse-ring { 0%{transform:scale(.5);opacity:.8} 100%{transform:scale(1.9);opacity:0} }
@media (prefers-reduced-motion: reduce) { .pulse-dot::after { animation: none; display: none; } }

/* ═══════════════════════════════════
   HERO — Apple-Stil
═══════════════════════════════════ */
.hero {
  padding: 72px 0 64px;
  text-align: center;
  position: relative;
  overflow: hidden;
}
/* Apple-style radial glow — sehr dezent */
.hero::before {
  content: '';
  position: absolute;
  top: -30%; left: 50%;
  transform: translateX(-50%);
  width: 120%; height: 70%;
  background: radial-gradient(ellipse, rgba(34,197,94,.07) 0%, transparent 65%);
  pointer-events: none;
}
/* Dezentes Dot-Grid (wie Referenz-Template) */
.hero::after {
  content: '';
  position: absolute; inset: 0;
  background-image: radial-gradient(circle, rgba(34,197,94,.12) 1px, transparent 1.2px);
  background-size: 28px 28px;
  -webkit-mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, #000 0%, transparent 100%);
  mask-image: radial-gradient(ellipse 80% 60% at 50% 30%, #000 0%, transparent 100%);
  pointer-events: none;
  opacity: .45;
}
.hero .wrap { position: relative; z-index: 2; max-width: 780px; }

.hero-badge {
  display: inline-flex; align-items: center; gap: 8px;
  background: var(--accent-soft); border: 1px solid var(--accent-line);
  border-radius: var(--r-pill);
  padding: 7px 16px 7px 10px;
  font-size: .79rem; font-weight: 600; color: var(--accent-deep);
  margin-bottom: 28px;
}

/* Headline — Apple-Format: groß, eng, präzise */
.hero h1 {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(2.8rem, 7vw, 5rem);
  line-height: 1.04; letter-spacing: -.04em;
  margin: 0 auto 18px;
  max-width: 14ch;
}
.hero h1 em {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--accent);
  letter-spacing: -.02em;
}
.hero-sub {
  font-size: clamp(1rem, 2vw, 1.15rem);
  color: var(--ink-2); line-height: 1.65;
  max-width: 46ch; margin: 0 auto 22px;
}

/* Garantie-Pill */
.g-pill {
  display: inline-flex; align-items: center; gap: 6px;
  background: var(--red-soft); border: 1px solid var(--red-line);
  color: #b91c1c; font-size: .76rem; font-weight: 700;
  padding: 5px 13px; border-radius: 6px; margin-bottom: 20px;
  letter-spacing: .01em;
}

/* Google-Bewertungen */
.google-row {
  display: flex; align-items: center; justify-content: center; gap: 10px;
  margin-bottom: 32px;
}
.stars { color: #f59e0b; letter-spacing: 2px; font-size: .92rem; }
.google-text { font-size: .77rem; color: var(--ink-2); }
.google-text strong { color: var(--ink); }

/* ═══════════════════════════════════
   FORM CARD — Glassmorphism
═══════════════════════════════════ */
.form-card {
  background: rgba(255,255,255,.88);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255,255,255,.75);
  border-radius: 22px;
  box-shadow:
    0 2px 4px rgba(0,0,0,.03),
    0 12px 36px rgba(0,0,0,.07),
    0 40px 80px rgba(0,0,0,.06),
    inset 0 1px 0 rgba(255,255,255,.9);
  padding: 36px 32px;
  max-width: 460px;
  margin: 0 auto;
  text-align: left;
}
.fp {
  display: flex; gap: 5px; margin-bottom: 28px;
}
.fp-dot {
  flex: 1; height: 3px; border-radius: 3px;
  background: var(--card-line); transition: background .35s;
}
.fp-dot.active { background: var(--accent); }
.fp-dot.done   { background: rgba(34,197,94,.4); }

.f-step { display: none; }
.f-step.visible { display: block; }
.f-q { font-size: 1.15rem; font-weight: 700; letter-spacing: -.02em; margin-bottom: 14px; line-height: 1.3; }
.f-input {
  width: 100%; background: rgba(248,250,252,.8);
  border: 1.5px solid var(--card-line);
  border-radius: 12px; color: var(--ink);
  font-family: var(--f-sans); font-size: 1.05rem;
  padding: 14px 16px; outline: none;
  transition: border-color .2s, box-shadow .2s, background .2s;
}
.f-input::placeholder { color: #c4cad3; }
.f-input:focus {
  border-color: var(--accent); background: var(--bg);
  box-shadow: 0 0 0 3px rgba(34,197,94,.12);
}
.f-input.err { border-color: var(--red); }
.f-hint { font-size: .73rem; color: #9ca3af; margin-top: 6px; }
.f-err { font-size: .74rem; color: var(--red); margin-top: 5px; display: none; }
.f-err.show { display: block; }
.f-btn {
  display: block; width: 100%; margin-top: 14px;
  background: var(--ink); color: #fff;
  font-family: var(--f-sans); font-weight: 600; font-size: 1rem;
  padding: 15px; border-radius: 12px; border: none; cursor: pointer;
  letter-spacing: -.01em;
  box-shadow: rgba(255,255,255,.10) 0 0 0 3px inset;
  transition: background .2s, transform .3s cubic-bezier(.34,1.56,.64,1), box-shadow .3s;
}
.f-btn:hover { background: #1a1a2e; transform: translateY(-2px); box-shadow: rgba(255,255,255,.10) 0 0 0 3px inset, rgba(0,0,0,.2) 0 8px 24px; }
.f-btn.green { background: var(--accent); }
.f-btn.green:hover { background: var(--accent-deep); box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset, rgba(34,197,94,.28) 0 8px 24px; }
.f-btn:disabled { opacity: .5; cursor: not-allowed; transform: none; box-shadow: none; }

.f-news { display: flex; gap: 10px; align-items: flex-start; margin-top: 14px; }
.f-news input[type="checkbox"] { width: 15px; height: 15px; margin-top: 2px; flex-shrink: 0; accent-color: var(--accent); cursor: pointer; }
.f-news label { font-size: .72rem; color: var(--ink-2); line-height: 1.5; cursor: pointer; }
.f-news label a { color: var(--accent-deep); text-decoration: underline; }

/* Thank You */
.f-ty { display: none; text-align: center; padding: 8px 0; }
.f-ty.visible { display: block; }
.f-ty-icon { color: var(--accent); width: 48px; height: 48px; margin: 0 auto 14px; }
.f-ty-h { font-size: 1.25rem; font-weight: 800; letter-spacing: -.02em; margin-bottom: 8px; }
.f-ty-p { font-size: .9rem; color: var(--ink-2); line-height: 1.6; max-width: 300px; margin: 0 auto; }
.f-ty-p strong { color: var(--accent-deep); }
.f-ty-hr { border: none; border-top: 1px solid var(--card-line); margin: 22px 0; }
.f-ty-profile {
  display: flex; align-items: center; gap: 13px;
  background: var(--bg-alt); border: 1px solid var(--card-line);
  border-radius: 14px; padding: 14px 16px; text-align: left; margin-bottom: 20px;
}
.f-ty-ava {
  width: 50px; height: 50px; border-radius: 50%; flex-shrink: 0;
  background: var(--accent-soft);
  display: flex; align-items: center; justify-content: center; overflow: hidden;
}
.f-ty-ava svg { color: var(--accent-deep); }
.f-ty-ava img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
.f-ty-name { font-size: .86rem; font-weight: 700; }
.f-ty-role { font-size: .73rem; color: var(--ink-2); margin-bottom: 3px; }
.f-ty-q    { font-size: .78rem; color: var(--ink); line-height: 1.5; font-style: italic; }
.f-ty-cal-l { font-size: .78rem; color: var(--ink-2); text-align: center; margin-bottom: 10px; }
.f-ty-cal-l strong { display: block; font-size: .9rem; color: var(--ink); margin-top: 1px; }

/* ═══════════════════════════════════
   TRUST PILLS (below form)
═══════════════════════════════════ */
.trust-row {
  display: flex; justify-content: center; gap: 20px; flex-wrap: wrap;
  margin-top: 16px;
}
.trust-pill {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: .74rem; color: var(--ink-2);
}
.trust-pill svg { color: var(--accent); }

/* ═══════════════════════════════════
   MOCKUP SECTION — Referenz exakt
═══════════════════════════════════ */
.mockup-section { padding: 40px 0 96px; }
.mockup-wrap {
  position: relative;
  max-width: 900px; margin: 0 auto; padding: 0 28px;
}
.mockup-outer {
  background: linear-gradient(160deg, #eef4f1, #fafdf9);
  background-image: radial-gradient(circle, rgba(34,197,94,.13) 1px, transparent 1.4px);
  background-size: 22px 22px;
  border: 1px solid rgba(0,0,0,.06);
  border-radius: 26px;
  padding: 24px;
  box-shadow: var(--shadow-xl);
}
.mockup-inner {
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 24px 28px;
  text-align: left;
}
.m-dots { display: flex; gap: 6px; margin-bottom: 20px; }
.m-dots span { width: 10px; height: 10px; border-radius: 50%; background: var(--card-line); }

/* Kalender-Inhalt */
.m-header {
  display: flex; align-items: center; justify-content: space-between; margin-bottom: 20px;
}
.m-live {
  display: flex; align-items: center; gap: 7px;
  font-size: .72rem; font-weight: 700; text-transform: uppercase;
  letter-spacing: .05em; color: var(--accent-deep);
}
.m-service { font-size: .84rem; font-weight: 700; }
.m-service small { display: block; font-size: .7rem; font-weight: 400; color: var(--ink-2); }
.m-avtr {
  width: 32px; height: 32px; border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent-deep));
  color: #fff; font-size: .72rem; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
}
.m-split { display: grid; grid-template-columns: 1fr 1px 180px; }
@media (max-width: 680px) { .m-split { grid-template-columns: 1fr; } }
.m-div { background: var(--line); margin: 0 22px; }
@media (max-width: 680px) { .m-div { display: none; } }
.m-cal { padding-right: 0; }
@media (max-width: 680px) { .m-cal { margin-bottom: 18px; } }
.m-month-row { display: flex; align-items: center; justify-content: space-between; margin-bottom: 12px; }
.m-month { font-size: .85rem; font-weight: 700; }
.m-navs { display: flex; gap: 4px; }
.m-nav {
  width: 24px; height: 24px; border: 1px solid var(--card-line);
  border-radius: 6px; background: none; cursor: pointer;
  color: var(--ink-2); font-size: .78rem;
  display: flex; align-items: center; justify-content: center;
  transition: background .15s;
}
.m-nav:hover { background: var(--accent-soft); }
.m-wdays { display: grid; grid-template-columns: repeat(7,1fr); text-align: center; margin-bottom: 4px; }
.m-wdays span { font-size: .62rem; font-weight: 600; color: #9ca3af; }
.m-days { display: grid; grid-template-columns: repeat(7,1fr); gap: 1px; text-align: center; }
.m-days span { padding: 5px 0; font-size: .74rem; border-radius: 6px; }
.m-days .av { color: var(--accent-deep); font-weight: 600; cursor: pointer; }
.m-days .av:hover { background: var(--accent-soft); }
.m-days .sel { background: var(--accent); color: #fff; font-weight: 700; }
.m-days .td { font-weight: 700; box-shadow: inset 0 0 0 1.5px var(--accent); border-radius: 6px; }
.m-days .dim { color: #d1d5db; }
.m-times-col {
  padding-left: 22px; display: flex; flex-direction: column; justify-content: center;
}
@media (max-width: 680px) { .m-times-col { padding-left: 0; border-top: 1px solid var(--line); padding-top: 14px; } }
.m-tlabel { font-size: .68rem; font-weight: 700; text-transform: uppercase; letter-spacing: .06em; color: #9ca3af; margin-bottom: 10px; }
.m-slots { display: flex; flex-direction: column; gap: 6px; }
.m-slot {
  padding: 8px 14px; border-radius: 9px; border: 1px solid var(--card-line);
  font-size: .79rem; background: none; cursor: pointer;
  text-align: center; transition: all .15s;
}
.m-slot:hover { border-color: var(--accent); color: var(--accent-deep); }
.m-slot.sel { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
.m-confirm {
  display: block; width: 100%; margin-top: 10px;
  background: var(--ink); color: #fff;
  font-family: var(--f-sans); font-size: .84rem; font-weight: 600;
  padding: 10px; border-radius: 9px; border: none; cursor: pointer;
  box-shadow: rgba(255,255,255,.08) 0 0 0 2px inset;
  transition: background .2s, transform .2s;
}
.m-confirm:hover { background: #1a1a2e; transform: translateY(-1px); }

/* Float-Cards — Referenz-exakt: Spring-Pop + tiefer Schatten */
.float-card {
  position: absolute;
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: 18px;
  box-shadow: 0 20px 48px rgba(13,15,26,.13), 0 4px 12px rgba(0,0,0,.06);
  padding: 16px 20px;
  opacity: 0; transform: translateY(16px) scale(.92);
  transition: opacity .5s cubic-bezier(.34,1.56,.64,1), transform .5s cubic-bezier(.34,1.56,.64,1);
  white-space: nowrap;
}
.mockup-wrap.is-visible .float-card { opacity: 1; transform: none; }
/* Stat card: Fraunces-Zahl */
.float-card.stat { top: 3%; left: -2.5%; transition-delay: .15s; }
.fc-num {
  font-family: var(--f-serif); font-size: 1.65rem; font-weight: 500;
  font-style: italic; color: var(--accent); line-height: 1.1; display: block;
}
.fc-lbl { font-size: .73rem; color: var(--ink-2); margin-top: 2px; display: block; line-height: 1.4; }
/* Notif card */
.float-card.notif { bottom: 5%; right: -2.5%; display: flex; align-items: flex-start; gap: 12px; max-width: 240px; transition-delay: .35s; }
.fc-ic {
  width: 36px; height: 36px; border-radius: 10px;
  background: var(--accent-soft);
  display: flex; align-items: center; justify-content: center; flex: none;
}
.fc-ic svg { color: var(--accent-deep); }
.fc-notif-title { font-size: .84rem; font-weight: 700; color: var(--ink); margin-bottom: 2px; }
.fc-notif-body  { font-size: .73rem; color: var(--ink-2); }
.fc-notif-time  { font-size: .68rem; color: var(--accent-deep); font-weight: 600; margin-top: 4px; }
@media (max-width: 700px) { .float-card { display: none; } }
@media (prefers-reduced-motion: reduce) { .float-card { opacity: 1; transform: none; transition: none; } }

/* ═══════════════════════════════════
   SECTION LAYOUT
═══════════════════════════════════ */
.section { padding: 90px 0; }
.section.alt { background: var(--bg-alt); }
.section-head { text-align: center; max-width: 600px; margin: 0 auto 56px; }
.section-head h2 {
  font-weight: 800; font-size: clamp(1.75rem, 3.5vw, 2.5rem);
  letter-spacing: -.03em; line-height: 1.12; margin-bottom: 12px;
}
.section-head h2 em {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--accent);
}
.section-head p { color: var(--ink-2); font-size: 1rem; line-height: 1.65; }

/* ═══════════════════════════════════
   PAIN CARDS — Keine Icons, nur SVG-Illustration
═══════════════════════════════════ */
.cards3 { display: grid; grid-template-columns: repeat(3,1fr); gap: 22px; }
@media (max-width: 820px) { .cards3 { grid-template-columns: 1fr; } }

.card-m {
  background: var(--bg);
  border: 1px solid var(--card-line);
  border-radius: var(--r-card);
  overflow: hidden;
  transition: transform .3s, box-shadow .3s;
}
.card-m:hover { transform: translateY(-4px); box-shadow: var(--shadow-lg); }
.card-m .illus-bg {
  height: 168px;
  background: linear-gradient(180deg, #f0f9f3 0%, #e8f5ec 50%, #ffffff 100%);
  display: flex; align-items: center; justify-content: center;
  padding: 16px 24px;
  overflow: visible;
}
.card-m .c-body { padding: 22px 24px 26px; }
.card-m h3 {
  font-family: var(--f-serif); font-weight: 500; font-style: italic;
  font-size: 1.2rem; letter-spacing: -.01em; color: var(--ink); margin-bottom: 8px;
}
.card-m p { font-size: .9rem; line-height: 1.65; color: var(--ink-2); }

/* SVG-Illustrationen: gestaffeltes Einblenden */
.ill-el { opacity: 0; transform: translateY(5px); transform-box: fill-box; transform-origin: center; }
.card-m.reveal.is-visible .ill-el { animation: ill-in .6s cubic-bezier(.16,1,.3,1) forwards; }
.card-m.reveal.is-visible .ill-el.d1 { animation-delay: .1s; }
.card-m.reveal.is-visible .ill-el.d2 { animation-delay: .22s; }
.card-m.reveal.is-visible .ill-el.d3 { animation-delay: .34s; }
.card-m.reveal.is-visible .ill-el.d4 { animation-delay: .46s; }
.card-m.reveal.is-visible .ill-el.d5 { animation-delay: .58s; }
@keyframes ill-in { to { opacity: 1; transform: none; } }
.ill-breathe { transform-box: fill-box; transform-origin: center; }
.card-m.reveal.is-visible .ill-breathe {
  animation: ill-in .6s cubic-bezier(.16,1,.3,1) .3s forwards, ill-breathe 3.5s ease-in-out 1.2s infinite;
}
@keyframes ill-breathe { 0%,100%{ transform: scale(1) } 50%{ transform: scale(1.05) } }
@media (prefers-reduced-motion: reduce) {
  .ill-el, .ill-breathe { opacity: 1; transform: none; animation: none !important; }
}

/* ═══════════════════════════════════
   SOLUTION GRID
═══════════════════════════════════ */
.sol-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 16px; }
@media (max-width: 640px) { .sol-grid { grid-template-columns: 1fr; } }
.sol-item {
  display: flex; gap: 14px; align-items: flex-start;
  background: var(--bg); border: 1px solid var(--card-line);
  border-radius: 14px; padding: 18px 20px;
  transition: box-shadow .25s;
}
.sol-item:hover { box-shadow: var(--shadow-sm); }
.sol-check {
  width: 28px; height: 28px; border-radius: 50%; flex-shrink: 0; margin-top: 1px;
  background: var(--accent-soft);
  display: flex; align-items: center; justify-content: center;
}
.sol-check svg { color: var(--accent-deep); }
.sol-item strong { font-size: .92rem; font-weight: 700; display: block; margin-bottom: 2px; }
.sol-item span   { font-size: .8rem; color: var(--ink-2); line-height: 1.45; }

/* ═══════════════════════════════════
   GUARANTEE
═══════════════════════════════════ */
.g-wrap { max-width: 640px; margin: 0 auto; }
.g-card {
  background: var(--bg);
  border: 1.5px solid var(--accent-line);
  border-radius: 22px;
  padding: 52px 44px;
  text-align: center;
  position: relative; overflow: hidden;
  box-shadow: 0 4px 12px rgba(34,197,94,.06), 0 20px 60px rgba(34,197,94,.04);
}
.g-card::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--accent-deep));
}
.g-icon { color: var(--accent); margin: 0 auto 18px; width: 52px; height: 52px; }
.g-card h2 {
  font-weight: 800; font-size: clamp(1.5rem, 3vw, 2.1rem);
  letter-spacing: -.03em; line-height: 1.12; margin-bottom: 14px;
}
.g-card h2 em { font-family: var(--f-serif); font-style: italic; font-weight: 500; color: var(--accent); }
.g-card p { color: var(--ink-2); font-size: .97rem; line-height: 1.65; max-width: 44ch; margin: 0 auto 30px; }
.g-btns { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }
@media (max-width: 480px) { .g-card { padding: 36px 24px; } }

/* ═══════════════════════════════════
   FOOTER — Premium Dark
═══════════════════════════════════ */
footer {
  background: #0d1117;
  position: relative; overflow: hidden;
}
/* Dezenter grüner Glow oben */
footer::before {
  content: '';
  position: absolute; top: 0; left: 50%; transform: translateX(-50%);
  width: 600px; height: 200px;
  background: radial-gradient(ellipse, rgba(34,197,94,.08) 0%, transparent 70%);
  pointer-events: none;
}
/* Grüne Akzentlinie oben */
.foot-accent {
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--accent), transparent);
  opacity: .5;
}
/* Haupt-Grid */
.foot-top {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 48px;
  padding: 52px 0 44px;
  border-bottom: 1px solid rgba(255,255,255,.07);
  position: relative; z-index: 2;
}
@media (max-width: 760px) { .foot-top { grid-template-columns: 1fr; gap: 32px; } }

/* Brand-Spalte */
.foot-brand {}
.foot-brand-name {
  display: flex; align-items: center; gap: 8px;
  font-weight: 800; font-size: 1.05rem; color: #fff;
  margin-bottom: 10px;
}
.foot-brand-dot { width: 10px; height: 10px; border-radius: 50%; background: var(--accent); }
.foot-tagline { font-size: .86rem; color: rgba(255,255,255,.45); line-height: 1.6; max-width: 26ch; margin-bottom: 18px; }
.foot-addr { font-size: .78rem; color: rgba(255,255,255,.3); line-height: 1.7; }

/* Link-Spalten */
.foot-col h4 {
  font-size: .7rem; font-weight: 700; text-transform: uppercase;
  letter-spacing: .1em; color: rgba(255,255,255,.35);
  margin-bottom: 18px;
}
.foot-col a {
  display: block; font-size: .86rem;
  color: rgba(255,255,255,.55); margin-bottom: 11px;
  transition: color .2s;
}
.foot-col a:hover { color: var(--accent); }

/* Bottom-Bar */
.foot-bottom {
  display: flex; justify-content: space-between; align-items: center;
  flex-wrap: wrap; gap: 10px;
  padding: 18px 0 20px;
  font-size: .75rem; color: rgba(255,255,255,.22);
  position: relative; z-index: 2;
}
.foot-bottom a { color: rgba(255,255,255,.3); transition: color .2s; }
.foot-bottom a:hover { color: rgba(255,255,255,.6); }

/* Wordmark im Hintergrund */
.foot-wordmark {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(3.5rem, 13vw, 10rem);
  color: rgba(255,255,255,.025);
  text-align: center; line-height: 1; letter-spacing: -.03em;
  margin-bottom: 0; user-select: none; pointer-events: none;
  position: relative; z-index: 1;
}

/* ═══════════════════════════════════
   RESPONSIVE
═══════════════════════════════════ */
@media (max-width: 520px) {
  .form-card { padding: 24px 18px; }
  .g-card { padding: 32px 20px; }
}

  </style>
</Fragment>


<!-- ─── URGENCY BAR ──────────────────────────── -->
<div class="urg">
  Nur noch <strong>1 freier Platz</strong> in deiner Region — danach ist es zu spät
</div>


<!-- ─── NAV ─────────────────────────────────── -->
<div class="nav-outer">
  <nav class="pill">
    <div class="nav-brand">
      <span class="nav-dot"></span>
      Fastra One
    </div>
    <ul>
      <li><a href="#problem">Problem</a></li>
      <li><a href="#loesung">Lösung</a></li>
      <li><a href="#garantie">Garantie</a></li>
    </ul>
    <button class="btn btn-dark btn-sm" onclick="scrollToForm()">
      Platz sichern
      <span class="btn-arrow">
        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
      </span>
    </button>
  </nav>
</div>


<!-- ─── HERO ─────────────────────────────────── -->
<section class="hero">
  <div class="wrap">

    <span class="hero-badge reveal">
      <span class="pulse-dot"></span>
      Exklusiv für GaLaBau-Betriebe in Deutschland
    </span>

    <h1 class="reveal d1">
      3 neue Aufträge.<br>
      30 Tage. <em>Garantiert.</em>
    </h1>

    <p class="hero-sub reveal d2">
      Wir bauen dir ein System, das jeden Monat planbar neue Kunden bringt.
      Oder du zahlst uns keinen Cent.
    </p>

    <div class="g-pill reveal d2">
      Kein Risiko — Garantie oder Geld zurück
    </div>

    <div class="google-row reveal d2">
      <span class="stars">★★★★★</span>
      <span class="google-text"><strong>4,9 / 5</strong> · Google-Bewertungen</span>
    </div>

    <!-- FORM CARD -->
    <div class="form-card reveal d3" id="heroForm">

      <div id="fFormArea">
        <div class="fp" id="fProgress">
          <div class="fp-dot active" id="fdot-0"></div>
          <div class="fp-dot" id="fdot-1"></div>
          <div class="fp-dot" id="fdot-2"></div>
          <div class="fp-dot" id="fdot-3"></div>
        </div>

        <!-- Schritt 0 -->
        <div class="f-step visible" id="fstep-0">
          <p class="f-q">Wie ist dein Vorname?</p>
          <input class="f-input" id="f-vn" type="text" placeholder="z.B. Thomas" autocomplete="given-name" />
          <p class="f-err" id="ferr-0">Bitte gib deinen Vornamen ein.</p>
          <button class="f-btn" onclick="fNext(0)">Weiter →</button>
        </div>

        <!-- Schritt 1 -->
        <div class="f-step" id="fstep-1">
          <p class="f-q">Und dein Nachname?</p>
          <input class="f-input" id="f-nn" type="text" placeholder="z.B. Müller" autocomplete="family-name" />
          <p class="f-err" id="ferr-1">Bitte gib deinen Nachnamen ein.</p>
          <button class="f-btn" onclick="fNext(1)">Weiter →</button>
        </div>

        <!-- Schritt 2 -->
        <div class="f-step" id="fstep-2">
          <p class="f-q">Deine Handynummer?</p>
          <input class="f-input" id="f-ph" type="tel" placeholder="z.B. 0162 1234567" autocomplete="tel" />
          <p class="f-err" id="ferr-2">Bitte gib eine gültige Nummer ein.</p>
          <p class="f-hint">Wir rufen dich persönlich an — kein Spam.</p>
          <button class="f-btn" onclick="fNext(2)">Weiter →</button>
        </div>

        <!-- Schritt 3 -->
        <div class="f-step" id="fstep-3">
          <p class="f-q">Deine E-Mail-Adresse?</p>
          <input class="f-input" id="f-em" type="email" placeholder="z.B. thomas@musterbau.de" autocomplete="email" />
          <p class="f-err" id="ferr-3">Bitte gib eine gültige E-Mail-Adresse ein.</p>
          <div class="f-news">
            <input type="checkbox" id="f-nl" />
            <label for="f-nl">
              Ich möchte kostenlose Tipps zur Auftragsgewinnung erhalten. (Optional ·
              <a href="/datenschutz" target="_blank">Datenschutz</a>)
            </label>
          </div>
          <button class="f-btn green" id="fSubmitBtn" onclick="fSubmit()">
            Jetzt kostenlos Platz sichern →
          </button>
        </div>
      </div>

      <!-- Thank You -->
      <div class="f-ty" id="fThankyou">
        <div class="f-ty-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" width="48" height="48">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="7 12 10.5 15.5 17 9"/>
          </svg>
        </div>
        <h2 class="f-ty-h">Perfekt — wir melden uns!</h2>
        <p class="f-ty-p">
          Deine Anfrage ist angekommen.
          Wir melden uns <strong>innerhalb von 24 Stunden</strong> persönlich bei dir.
        </p>
        <hr class="f-ty-hr" />
        <div class="f-ty-profile">
          <div class="f-ty-ava" id="paulAvatarBox">
            <!-- Foto folgt: <img src="..." alt="Paul Fasterding"> -->
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>
          </div>
          <div>
            <div class="f-ty-role">Gründer · Fastra One, Hannover</div>
            <div class="f-ty-name">Paul Fasterding</div>
            <p class="f-ty-q">"Ich freue mich auf unser Gespräch."</p>
          </div>
        </div>
        <p class="f-ty-cal-l">
          Lieber direkt Termin buchen?
          <strong>Wähl einfach eine Zeit</strong>
        </p>
        <div
          class="calendly-inline-widget"
          data-url="https://calendly.com/paul-web/30min?hide_gdpr_banner=1&primary_color=22c55e"
          style="min-width:280px; height:640px;"
        ></div>
        <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
      </div>

    </div><!-- /form-card -->

    <div class="trust-row reveal d3">
      <span class="trust-pill">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        Kostenlos &amp; unverbindlich
      </span>
      <span class="trust-pill">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        Antwort in 24h
      </span>
      <span class="trust-pill">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        1 Betrieb pro Region
      </span>
    </div>

  </div>
</section>


<!-- ─── MOCKUP / KALENDER ─────────────────────── -->
<section class="mockup-section">
  <div class="wrap">
    <div class="mockup-wrap reveal">
      <div class="mockup-outer">
        <div class="mockup-inner">

          <div class="m-dots"><span></span><span></span><span></span></div>

          <div class="m-header">
            <div class="m-live">
              <span class="pulse-dot"></span>
              Live Buchungen
            </div>
            <div class="m-service">
              Termin buchen
              <small>GaLaBau-Beratung · 30 Min · Kostenlos</small>
            </div>
            <div class="m-avtr">FO</div>
          </div>

          <div class="m-split">
            <div class="m-cal">
              <div class="m-month-row">
                <span class="m-month">August 2026</span>
                <div class="m-navs">
                  <button class="m-nav">‹</button>
                  <button class="m-nav">›</button>
                </div>
              </div>
              <div class="m-wdays">
                <span>Mo</span><span>Di</span><span>Mi</span>
                <span>Do</span><span>Fr</span><span>Sa</span><span>So</span>
              </div>
              <div class="m-days">
                <span class="dim"></span><span class="dim"></span><span class="dim"></span>
                <span class="dim"></span><span class="av">1</span><span class="dim">2</span><span class="dim">3</span>
                <span class="av">4</span><span class="av">5</span><span class="av">6</span>
                <span class="av">7</span><span class="av">8</span><span class="dim">9</span><span class="dim">10</span>
                <span class="av">11</span><span class="av">12</span><span class="td">13</span>
                <span class="sel">14</span><span class="av">15</span><span class="dim">16</span><span class="dim">17</span>
                <span class="av">18</span><span class="av">19</span><span class="av">20</span>
                <span class="av">21</span><span class="av">22</span><span class="dim">23</span><span class="dim">24</span>
                <span class="av">25</span><span class="av">26</span><span class="av">27</span>
                <span class="av">28</span><span class="av">29</span><span class="dim">30</span><span class="dim">31</span>
              </div>
            </div>
            <div class="m-div"></div>
            <div class="m-times-col">
              <div class="m-tlabel">Do, 14. August</div>
              <div class="m-slots">
                <button class="m-slot">09:00 Uhr</button>
                <button class="m-slot sel">10:30 Uhr</button>
                <button class="m-slot">14:00 Uhr</button>
                <button class="m-slot">15:30 Uhr</button>
              </div>
              <button class="m-confirm">Termin bestätigen →</button>
            </div>
          </div>

        </div>
      </div>

      <!-- Float: Umsatz-Stat (Fraunces kursiv, links oben) -->
      <div class="float-card stat">
        <span class="fc-num">21.000 €</span>
        <span class="fc-lbl">Umsatz / Monat durch<br>neue Kunden</span>
      </div>

      <!-- Float: Buchungsbestätigung (rechts unten) -->
      <div class="float-card notif">
        <div class="fc-ic">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div>
          <div class="fc-notif-title">Booking bestätigt</div>
          <div class="fc-notif-body">Thomas M. · Mo, 14. Aug</div>
          <div class="fc-notif-time">10:30 Uhr · GaLaBau-Beratung</div>
        </div>
      </div>

    </div>
  </div>
</section>


<!-- ─── PROBLEM ───────────────────────────────── -->
<section class="section alt" id="problem">
  <div class="wrap">

    <div class="section-head reveal">
      <h2>Drei Gründe, warum dein Kalender <em>leer bleibt</em></h2>
      <p>Erkennst du dich wieder? Fast jeder GaLaBau-Betrieb kämpft mit denselben drei Mustern.</p>
    </div>

    <div class="cards3">

      <!-- KARTE 1: Person mit Smartphone + Chat-Bubbles -->
      <div class="card-m reveal">
        <div class="illus-bg">
          <svg viewBox="0 0 280 136" fill="none" xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
            <defs>
              <filter id="ph-shadow" x="-30%" y="-30%" width="160%" height="160%">
                <feDropShadow dx="0" dy="4" stdDeviation="8" flood-color="#00000018"/>
              </filter>
              <filter id="badge-sh" x="-15%" y="-30%" width="130%" height="160%">
                <feDropShadow dx="0" dy="2" stdDeviation="6" flood-color="#00000014"/>
              </filter>
            </defs>
            <!-- Telefon (zentriert) -->
            <g class="ill-el d2" filter="url(#ph-shadow)">
              <rect x="109" y="12" width="62" height="108" rx="11" fill="white" stroke="#e5e7eb" stroke-width="1.2"/>
              <!-- Screen -->
              <rect x="115" y="26" width="50" height="82" rx="5" fill="#f9fafb"/>
              <!-- Kamera -->
              <circle cx="140" cy="20" r="2.5" fill="#d1d5db"/>
              <!-- Chat-Bubbles auf Screen -->
              <rect x="118" y="32" width="30" height="10" rx="5" fill="#22c55e" class="ill-el d3"/>
              <rect x="134" y="47" width="28" height="10" rx="5" fill="#e5e7eb" class="ill-el d3"/>
              <rect x="118" y="62" width="34" height="10" rx="5" fill="#22c55e" class="ill-el d4"/>
              <rect x="134" y="77" width="24" height="10" rx="5" fill="#e5e7eb" class="ill-el d4"/>
              <!-- Home-Indicator -->
              <rect x="130" y="110" width="20" height="3" rx="1.5" fill="#e5e7eb"/>
            </g>
            <!-- Badge 1: Neue Anfrage (links oben) -->
            <g class="ill-el ill-breathe d1" filter="url(#badge-sh)">
              <rect x="4" y="14" width="98" height="28" rx="14" fill="white"/>
              <circle cx="20" cy="28" r="9" fill="#22c55e"/>
              <path d="M16.5 28.5l2.5 2.5 4.5-5" stroke="white" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
              <text x="36" y="32.5" font-family="Inter,-apple-system,sans-serif" font-size="10" fill="#111111" font-weight="600">Neue Anfrage</text>
            </g>
            <!-- Badge 2: Neuer Auftrag (rechts oben) -->
            <g class="ill-el d2" filter="url(#badge-sh)">
              <rect x="178" y="8" width="98" height="28" rx="14" fill="white"/>
              <circle cx="194" cy="22" r="9" fill="#22c55e"/>
              <path d="M190.5 22.5l2.5 2.5 4.5-5" stroke="white" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
              <text x="210" y="26.5" font-family="Inter,-apple-system,sans-serif" font-size="10" fill="#111111" font-weight="600">Neuer Auftrag</text>
            </g>
            <!-- Badge 3: Termin bestätigt (rechts unten) -->
            <g class="ill-el d3" filter="url(#badge-sh)">
              <rect x="160" y="102" width="116" height="28" rx="14" fill="white"/>
              <circle cx="176" cy="116" r="9" fill="#22c55e"/>
              <path d="M172.5 116.5l2.5 2.5 4.5-5" stroke="white" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
              <text x="192" y="120.5" font-family="Inter,-apple-system,sans-serif" font-size="10" fill="#111111" font-weight="600">Termin bestätigt</text>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>Keine planbaren Aufträge</h3>
          <p>Du weißt nicht, wann das nächste Telefon klingelt. Das System fehlt — nicht der Wille.</p>
        </div>
      </div>

      <!-- KARTE 2: Mundpropaganda mit rotem X -->
      <div class="card-m reveal d1">
        <div class="illus-bg">
          <svg viewBox="0 0 280 136" fill="none" xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
            <defs>
              <filter id="p-shadow" x="-20%" y="-20%" width="140%" height="140%">
                <feDropShadow dx="0" dy="3" stdDeviation="7" flood-color="#00000012"/>
              </filter>
            </defs>
            <!-- Person 1 (links) -->
            <g class="ill-el d1">
              <!-- Kopf -->
              <circle cx="68" cy="40" r="20" fill="#e5e7eb" filter="url(#p-shadow)"/>
              <!-- Körper -->
              <rect x="50" y="65" width="36" height="50" rx="18" fill="#e5e7eb"/>
            </g>
            <!-- Person 2 (rechts) -->
            <g class="ill-el d1">
              <circle cx="212" cy="40" r="20" fill="#e5e7eb" filter="url(#p-shadow)"/>
              <rect x="194" y="65" width="36" height="50" rx="18" fill="#e5e7eb"/>
            </g>
            <!-- Sprechblasen (klein) -->
            <g class="ill-el d2">
              <rect x="92" y="28" width="40" height="18" rx="9" fill="white" stroke="#e5e7eb" stroke-width="1"/>
              <path d="M96 46l-8 6 12-2z" fill="white" stroke="#e5e7eb" stroke-width="0.5"/>
              <rect x="148" y="28" width="40" height="18" rx="9" fill="white" stroke="#e5e7eb" stroke-width="1"/>
              <path d="M184 46l8 6-12-2z" fill="white" stroke="#e5e7eb" stroke-width="0.5"/>
            </g>
            <!-- Rotes X (Kreis + Linien) -->
            <g class="ill-el d3">
              <circle cx="140" cy="68" r="34" fill="rgba(239,68,68,.07)" stroke="#fca5a5" stroke-width="1.5"/>
              <line x1="120" y1="48" x2="160" y2="88" stroke="#ef4444" stroke-width="5.5" stroke-linecap="round"/>
              <line x1="160" y1="48" x2="120" y2="88" stroke="#ef4444" stroke-width="5.5" stroke-linecap="round"/>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>Nur Mundpropaganda</h3>
          <p>Klappt jahrelang — bis es plötzlich nicht mehr klappt. Und kein Plan B da ist.</p>
        </div>
      </div>

      <!-- KARTE 3: Winter-Barchart mit sinkenden Balken -->
      <div class="card-m reveal d2">
        <div class="illus-bg">
          <svg viewBox="0 0 280 136" fill="none" xmlns="http://www.w3.org/2000/svg" width="100%" height="100%">
            <defs>
              <filter id="bar-glow" x="-20%" y="-20%" width="140%" height="140%">
                <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#22c55e22"/>
              </filter>
            </defs>
            <!-- Achse Y -->
            <line x1="36" y1="12" x2="36" y2="104" stroke="#e5e7eb" stroke-width="1.5"/>
            <!-- Achse X -->
            <line x1="36" y1="104" x2="264" y2="104" stroke="#e5e7eb" stroke-width="1.5"/>
            <!-- Grid-Linien -->
            <line x1="36" y1="40" x2="264" y2="40" stroke="#f3f4f6" stroke-width="1" stroke-dasharray="4 4"/>
            <line x1="36" y1="65" x2="264" y2="65" stroke="#f3f4f6" stroke-width="1" stroke-dasharray="4 4"/>
            <line x1="36" y1="87" x2="264" y2="87" stroke="#f3f4f6" stroke-width="1" stroke-dasharray="4 4"/>

            <!-- Balken: Jan–Aug (grün, steigend) -->
            <g class="ill-el d1" filter="url(#bar-glow)">
              <rect x="43" y="88" width="19" height="16" rx="4" fill="#bbf7d0"/>
              <rect x="68" y="78" width="19" height="26" rx="4" fill="#86efac"/>
              <rect x="93" y="60" width="19" height="44" rx="4" fill="#4ade80"/>
              <rect x="118" y="44" width="19" height="60" rx="4" fill="#22c55e"/>
              <rect x="143" y="30" width="19" height="74" rx="4" fill="#22c55e"/>
              <rect x="168" y="38" width="19" height="66" rx="4" fill="#22c55e"/>
            </g>

            <!-- Balken: Sep–Dez (rot, sinkend) -->
            <g class="ill-el d3">
              <rect x="193" y="72" width="19" height="32" rx="4" fill="#fca5a5"/>
              <rect x="218" y="84" width="19" height="20" rx="4" fill="#f87171"/>
              <rect x="243" y="92" width="19" height="12" rx="4" fill="#ef4444"/>
            </g>

            <!-- Rote Trendlinie (Herbst→Winter) -->
            <g class="ill-el d4">
              <polyline
                points="202,72 227,84 252,92"
                stroke="#ef4444" stroke-width="2.5"
                stroke-linecap="round" stroke-linejoin="round"
                fill="none"
              />
              <!-- Pfeil am Ende -->
              <path d="M247 90 L252 95 L257 90" stroke="#ef4444" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
            </g>

            <!-- Minus-Symbol über Winter-Balken -->
            <g class="ill-el d5">
              <rect x="219" y="70" width="16" height="3" rx="1.5" fill="#ef4444"/>
              <!-- kleines "-" Label -->
              <text x="230" y="68" font-family="Inter,sans-serif" font-size="9" fill="#ef4444" font-weight="700" text-anchor="middle">–40%</text>
            </g>

            <!-- Monats-Labels -->
            <g fill="#9ca3af" font-family="Inter,-apple-system,sans-serif" font-size="8">
              <text x="52"  y="117" text-anchor="middle">Jan</text>
              <text x="77"  y="117" text-anchor="middle">Feb</text>
              <text x="102" y="117" text-anchor="middle">Mrz</text>
              <text x="127" y="117" text-anchor="middle">Jun</text>
              <text x="152" y="117" text-anchor="middle">Aug</text>
              <text x="177" y="117" text-anchor="middle">Sep</text>
              <text x="202" y="117" text-anchor="middle" fill="#f87171">Okt</text>
              <text x="227" y="117" text-anchor="middle" fill="#ef4444">Nov</text>
              <text x="252" y="117" text-anchor="middle" fill="#ef4444">Dez</text>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>Oktober, November, Dezember</h3>
          <p>Der Umsatz bricht ein. Mitarbeiter, Fixkosten — du zahlst aus der Substanz.</p>
        </div>
      </div>

    </div>
  </div>
</section>


<!-- ─── LÖSUNG ────────────────────────────────── -->
<section class="section" id="loesung">
  <div class="wrap">
    <div class="section-head reveal">
      <h2>Dein <em>Auftragssystem</em> — fertig gebaut.</h2>
      <p>Alles aus einer Hand. Wir bauen es. Du nutzt es. Du bekommst die Aufträge.</p>
    </div>
    <div class="sol-grid">
      <div class="sol-item reveal">
        <div class="sol-check">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div>
          <strong>Webseite die Anfragen bringt</strong>
          <span>Speziell für GaLaBau-Betriebe — kein Standard-Template.</span>
        </div>
      </div>
      <div class="sol-item reveal d1">
        <div class="sol-check">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div>
          <strong>Google Ads in deiner Region</strong>
          <span>Wir schalten und optimieren. Du bekommst die Anfragen.</span>
        </div>
      </div>
      <div class="sol-item reveal d1">
        <div class="sol-check">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div>
          <strong>Live-Dashboard mit deinen Anfragen</strong>
          <span>Name, Nummer, Projekt — sofort auf dein Handy.</span>
        </div>
      </div>
      <div class="sol-item reveal d2">
        <div class="sol-check">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
        </div>
        <div>
          <strong>Exklusivität in deiner Region</strong>
          <span>Nur 1 GaLaBauer pro Region. Dein Wettbewerber bleibt draußen.</span>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ─── GARANTIE ──────────────────────────────── -->
<section class="section alt" id="garantie">
  <div class="wrap">
    <div class="g-wrap">
      <div class="g-card reveal">
        <div class="g-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" width="52" height="52">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <polyline points="9 12 11.5 14.5 15 10"/>
          </svg>
        </div>
        <h2>
          <em>3 Aufträge in 30 Tagen</em><br>
          oder du zahlst nichts.
        </h2>
        <p>
          Wenn du nach 30 Tagen keine 3 qualifizierten Auftragsanfragen erhältst,
          zahlst du uns keinen Cent. Das Risiko trägt komplett Fastra One.
        </p>
        <div class="g-btns">
          <button class="btn btn-primary" onclick="scrollToForm()">
            Jetzt kostenlos anfragen
            <span class="btn-arrow">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            </span>
          </button>
          <a href="https://calendly.com/paul-web/30min" target="_blank" rel="noopener" class="btn btn-outline">
            Direkt Termin buchen
          </a>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ─── FOOTER ────────────────────────────────── -->
<footer>

  <div class="foot-accent"></div>

  <div class="wrap">
    <div class="foot-top">

      <!-- Brand -->
      <div class="foot-brand">
        <div class="foot-brand-name">
          <span class="foot-brand-dot"></span>
          Fastra One
        </div>
        <p class="foot-tagline">
          Wir bauen Systeme, die automatisch neue Kunden gewinnen —
          für Selbstständige im GaLaBau.
        </p>
        <address class="foot-addr">
          Paul Niklas Fasterding<br>
          Am Flachsgraben 9<br>
          30657 Hannover
        </address>
      </div>

      <!-- Rechtliches -->
      <div class="foot-col">
        <h4>Rechtliches</h4>
        <a href="/impressum">Impressum</a>
        <a href="/datenschutz">Datenschutzerklärung</a>
        <a href="/datenschutz#cookies">Cookie-Richtlinien</a>
        <a href="/agb">AGB</a>
      </div>

      <!-- Kontakt / Links -->
      <div class="foot-col">
        <h4>Kontakt</h4>
        <a href="https://calendly.com/paul-web/30min" target="_blank" rel="noopener">Termin buchen</a>
        <a href="mailto:paul.web@outlook.de">E-Mail schreiben</a>
        <a href="https://fastraone.de" target="_blank" rel="noopener">fastraone.de</a>
      </div>

    </div>

    <div class="foot-bottom">
      <span>© 2026 Fastra One · Einzelunternehmen · Kleinunternehmer gem. § 19 UStG</span>
      <div style="display:flex; gap:16px;">
        <a href="/impressum">Impressum</a>
        <a href="/datenschutz">Datenschutz</a>
      </div>
    </div>
  </div>

  <div class="foot-wordmark" aria-hidden="true">Fastra One</div>

</footer>


<script is:inline>

var _fd = { vn:'', nn:'', ph:'', em:'', nl:false };
var _step = 0;

function scrollToForm() {
  document.getElementById('heroForm').scrollIntoView({ behavior:'smooth', block:'center' });
  setTimeout(function(){ var el = document.getElementById('f-vn'); if(el) el.focus(); }, 450);
}

function fUpdateProg(s) {
  for (var i=0; i<4; i++) {
    var d = document.getElementById('fdot-'+i);
    d.className = 'fp-dot' + (i < s ? ' done' : i === s ? ' active' : '');
  }
}

function fShowStep(n) {
  for (var i=0; i<4; i++) {
    var s = document.getElementById('fstep-'+i);
    if (s) s.className = 'f-step' + (i===n ? ' visible' : '');
  }
  fUpdateProg(n); _step = n;
  var ids = ['f-vn','f-nn','f-ph','f-em'];
  var inp = document.getElementById(ids[n]);
  if (inp) setTimeout(function(){ inp.focus(); }, 60);
}

function fNext(cur) {
  var ids  = ['f-vn','f-nn','f-ph','f-em'];
  var keys = ['vn','nn','ph','em'];
  var val  = document.getElementById(ids[cur]).value.trim();
  var errEl = document.getElementById('ferr-'+cur);
  var ok = cur===0 ? val.length>=2
         : cur===1 ? val.length>=2
         : cur===2 ? val.replace(/\s/g,'').length>=6
         : /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val);
  if (!ok) {
    errEl.className = 'f-err show';
    document.getElementById(ids[cur]).classList.add('err');
    return;
  }
  errEl.className = 'f-err';
  document.getElementById(ids[cur]).classList.remove('err');
  _fd[keys[cur]] = val;
  fShowStep(cur + 1);
}

function fSubmit() {
  var em = document.getElementById('f-em').value.trim();
  var errEl = document.getElementById('ferr-3');
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(em)) {
    errEl.className = 'f-err show';
    document.getElementById('f-em').classList.add('err');
    return;
  }
  _fd.em = em;
  _fd.nl = document.getElementById('f-nl').checked;
  var btn = document.getElementById('fSubmitBtn');
  btn.disabled = true; btn.textContent = 'Wird gesendet…';
  var p = new URLSearchParams(window.location.search);
  var payload = {
    name: _fd.vn + ' ' + _fd.nn,
    vorname: _fd.vn, nachname: _fd.nn,
    phone: _fd.ph, email: _fd.em, newsletter: _fd.nl,
    seite: 'galabau-garantie',
    utm_source:   p.get('utm_source')   || '',
    utm_medium:   p.get('utm_medium')   || '',
    utm_campaign: p.get('utm_campaign') || '',
    utm_content:  p.get('utm_content')  || '',
    created_at: new Date().toISOString()
  };
  fetch('https://fastraone-admin.vercel.app/api/leads', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify(payload)
  })
  .then(function(r){ return r.ok ? r : Promise.reject(r.status); })
  .then(function(){
    if (window.dataLayer) window.dataLayer.push({event:'lead_submit', lead_seite:'galabau-garantie'});
    fShowTY();
  })
  .catch(function(){ fShowTY(); });
}

function fShowTY() {
  document.getElementById('fFormArea').style.display = 'none';
  document.getElementById('fThankyou').className = 'f-ty visible';
}

document.addEventListener('keydown', function(e){
  if (e.key !== 'Enter') return;
  var a = [function(){fNext(0);},function(){fNext(1);},function(){fNext(2);},function(){fSubmit();}];
  if (a[_step]) a[_step]();
});

/* Scroll Reveal */
(function(){
  var els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) {
    els.forEach(function(e){ e.classList.add('is-visible'); }); return;
  }
  var io = new IntersectionObserver(function(entries){
    entries.forEach(function(entry){
      if (entry.isIntersecting){
        entry.target.classList.add('is-visible');
        io.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });
  els.forEach(function(e){ io.observe(e); });
})();

</script>

</BaseLayout>
