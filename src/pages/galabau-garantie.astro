---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="3 GaLaBau-Aufträge in 30 Tagen — Garantiert | Fastra One"
  description="Wir garantieren dir 3 qualifizierte Auftragsanfragen in 30 Tagen — oder du zahlst nichts. Exklusiv für GaLaBau-Betriebe."
  navVariant="none"
  footerVariant="none"
>

<Fragment slot="head">
  <meta name="robots" content="noindex, nofollow" />
  <!-- Fraunces: Signatur-Serifen-Schrift für Akzent-Wörter in Headlines -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@1,9..144,500&display=swap" rel="stylesheet" />
  <style is:global>

/* ═══════════════════════════════════════════════
   TOKENS
═══════════════════════════════════════════════ */
:root {
  --accent:      #22c55e;
  --accent-deep: #15803d;
  --accent-soft: rgba(34,197,94,.10);
  --accent-line: rgba(34,197,94,.25);
  --ink:         #111111;
  --bg:          #ffffff;
  --white:       #ffffff;
  --gray:        #6b7280;
  --gray-light:  #9ca3af;
  --line:        rgba(0,0,0,.07);
  --card-line:   #e5e7eb;
  --red:         #ef4444;
  --red-soft:    rgba(239,68,68,.08);
  --red-line:    rgba(239,68,68,.25);
  --f-serif:     'Fraunces', Georgia, serif;
  --f-sans:      'Inter', -apple-system, sans-serif;
  --radius-pill: 100px;
  --radius-card: 16px;
  --radius-sm:   10px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html  { scroll-behavior: smooth; }
body  { font-family: var(--f-sans); background: var(--bg); color: var(--ink);
        -webkit-font-smoothing: antialiased; overflow-x: hidden; }
a     { color: inherit; text-decoration: none; }
img   { max-width: 100%; display: block; }
:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; }
.wrap { max-width: 1100px; margin: 0 auto; padding: 0 24px; }

/* ═══════════════════════════════════════════════
   REVEAL ANIMATIONS
═══════════════════════════════════════════════ */
.reveal { opacity: 0; transform: translateY(24px);
          transition: opacity .6s ease, transform .6s ease; }
.reveal.is-visible { opacity: 1; transform: none; }
.reveal.delay-1 { transition-delay: .12s; }
.reveal.delay-2 { transition-delay: .24s; }
.reveal.delay-3 { transition-delay: .36s; }
@media (prefers-reduced-motion: reduce) {
  .reveal { opacity: 1; transform: none; transition: none; }
}

/* ═══════════════════════════════════════════════
   URGENCY BAR
═══════════════════════════════════════════════ */
.urg-bar {
  background: var(--red);
  color: #fff;
  text-align: center;
  padding: 10px 20px;
  font-size: .81rem;
  font-weight: 700;
  letter-spacing: .02em;
}

/* ═══════════════════════════════════════════════
   NAV — Pill-Stil (nachgebaut nach Faris-Schmidt)
═══════════════════════════════════════════════ */
.nav-outer {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  justify-content: center;
  padding: 12px 16px;
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}
nav.pill {
  width: 100%; max-width: 1060px;
  display: flex; align-items: center; justify-content: space-between;
  background: var(--white);
  border-radius: var(--radius-pill);
  padding: 12px 12px 12px 24px;
  box-shadow: 0 1px 3px rgba(0,0,0,.06), 0 4px 16px rgba(0,0,0,.04);
}
.nav-brand {
  display: flex; align-items: center; gap: 9px;
  font-weight: 800; font-size: 1rem; letter-spacing: -.02em;
}
.nav-dot {
  width: 11px; height: 11px; border-radius: 50%;
  background: var(--accent);
}
nav.pill ul {
  display: flex; gap: 28px; list-style: none;
}
nav.pill ul a {
  font-size: .88rem; font-weight: 500; color: var(--gray);
  transition: color .2s;
}
nav.pill ul a:hover { color: var(--ink); }
@media (max-width: 720px) { nav.pill ul { display: none; } }

/* Buttons — exakt nach Referenz: asymm. Padding, Inset-Lichtring, Hover-Glow */
.btn {
  display: inline-flex; align-items: center; gap: 8px;
  border-radius: var(--radius-pill);
  font-family: var(--f-sans); font-weight: 400;
  font-size: .92rem; letter-spacing: -.02em;
  padding: 11px 11px 11px 20px;
  white-space: nowrap; border: none; cursor: pointer;
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset;
  transition: transform .3s, box-shadow .3s;
}
.btn:hover { transform: translateY(-2px); }
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover {
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset,
              rgba(34,197,94,.35) 0 8px 24px;
}
.btn-dark { background: var(--ink); color: #fff; }
.btn-dark:hover {
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset,
              rgba(0,0,0,.25) 0 8px 24px;
}
.btn-outline {
  background: var(--white); color: var(--ink);
  border: 1px solid var(--card-line); box-shadow: none;
  padding: 11px 20px;
}
.btn-outline:hover { box-shadow: 0 8px 20px rgba(0,0,0,.08); }
.btn-arrow {
  width: 28px; height: 28px; border-radius: 50%;
  background: var(--white); padding: 6px;
  display: inline-flex; align-items: center; justify-content: center;
  flex: none;
}
.btn-primary .btn-arrow { color: var(--accent); }
.btn-dark .btn-arrow { color: var(--ink); }
.btn-sm {
  font-size: .82rem; padding: 9px 9px 9px 16px;
}
.btn-sm .btn-arrow { width: 24px; height: 24px; padding: 5px; }

/* ═══════════════════════════════════════════════
   PULSE DOT (Signatur-Detail "live")
═══════════════════════════════════════════════ */
.pulse-dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--accent); position: relative; flex: none;
}
.pulse-dot::after {
  content: ''; position: absolute; inset: -5px;
  border-radius: 50%; border: 2px solid var(--accent);
  animation: pulse-ring 2s ease infinite;
}
@keyframes pulse-ring {
  0%   { transform: scale(.55); opacity: .8; }
  100% { transform: scale(1.8);  opacity: 0; }
}
@media (prefers-reduced-motion: reduce) {
  .pulse-dot::after { animation: none; display: none; }
}

/* ═══════════════════════════════════════════════
   HERO
═══════════════════════════════════════════════ */
.hero {
  padding: 72px 0 60px;
  text-align: center;
}
.hero .wrap {
  max-width: 760px;
}
.hero-badge {
  display: inline-flex; align-items: center; gap: 8px;
  background: var(--accent-soft); border: 1px solid var(--accent-line);
  border-radius: var(--radius-pill);
  padding: 8px 18px 8px 12px;
  font-size: .8rem; font-weight: 600; color: var(--accent-deep);
  margin-bottom: 26px;
}
.hero h1 {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(2.4rem, 6vw, 4.2rem);
  line-height: 1.06; letter-spacing: -.03em;
  max-width: 18ch; margin: 0 auto 10px;
}
.hero h1 em {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--accent);
}
.hero h1 .serif-hook {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--gray); font-size: .7em;
  display: block; margin-bottom: 4px; letter-spacing: -.01em;
}
.hero-sub {
  color: var(--gray); font-size: 1.05rem;
  max-width: 46ch; margin: 0 auto 10px; line-height: 1.65;
}
.hero-sub strong { color: var(--ink); font-weight: 600; }

/* Guarantee pill (rote Variante) */
.guarantee-pill {
  display: inline-flex; align-items: center; gap: 6px;
  background: var(--red-soft); border: 1px solid var(--red-line);
  color: #b91c1c; font-size: .77rem; font-weight: 700;
  padding: 5px 14px; border-radius: 6px; margin: 12px 0 22px;
  letter-spacing: .01em;
}

/* Google Bewertungen */
.google-row {
  display: flex; align-items: center; justify-content: center; gap: 10px;
  margin-bottom: 36px;
}
.google-stars { color: #f59e0b; letter-spacing: 2px; font-size: .95rem; }
.google-text { font-size: .78rem; color: var(--gray); }
.google-text strong { color: var(--ink); }

/* ═══════════════════════════════════════════════
   FORM CARD
═══════════════════════════════════════════════ */
.form-card {
  background: var(--white);
  border: 1px solid var(--card-line);
  border-radius: 20px;
  box-shadow: 0 4px 12px rgba(0,0,0,.05), 0 24px 64px rgba(0,0,0,.09);
  padding: 36px 32px;
  max-width: 480px;
  margin: 0 auto;
  text-align: left;
}
.form-progress {
  display: flex; gap: 6px; margin-bottom: 28px;
}
.form-progress-dot {
  flex: 1; height: 3px; border-radius: 3px;
  background: var(--card-line); transition: background .3s;
}
.form-progress-dot.active { background: var(--accent); }
.form-progress-dot.done   { background: rgba(34,197,94,.45); }

.f-step { display: none; }
.f-step.visible { display: block; }

.f-question {
  font-size: 1.2rem; font-weight: 700; letter-spacing: -.02em;
  color: var(--ink); margin-bottom: 16px; line-height: 1.3;
}
.f-input {
  width: 100%;
  background: var(--bg);
  border: 1.5px solid var(--card-line);
  border-radius: 10px;
  color: var(--ink);
  font-family: var(--f-sans);
  font-size: 1.05rem;
  padding: 14px 16px;
  outline: none;
  transition: border-color .2s, box-shadow .2s;
  appearance: none;
}
.f-input::placeholder { color: #c4cad3; }
.f-input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(34,197,94,.15);
}
.f-input.error { border-color: var(--red); }
.f-hint {
  font-size: .74rem; color: var(--gray-light); margin-top: 7px;
}
.f-err {
  font-size: .75rem; color: var(--red);
  margin-top: 6px; display: none;
}
.f-err.show { display: block; }

.f-next-btn {
  display: block; width: 100%; margin-top: 14px;
  background: var(--accent); color: #fff;
  font-family: var(--f-sans); font-weight: 600; font-size: 1rem;
  padding: 15px; border-radius: 10px; border: none; cursor: pointer;
  letter-spacing: -.01em;
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset;
  transition: background .2s, transform .25s, box-shadow .25s;
}
.f-next-btn:hover {
  background: var(--accent-deep);
  transform: translateY(-2px);
  box-shadow: rgba(255,255,255,.12) 0 0 0 3px inset,
              rgba(34,197,94,.3) 0 8px 24px;
}
.f-next-btn:disabled { opacity: .5; cursor: not-allowed; transform: none; box-shadow: none; }

.f-newsletter-row {
  display: flex; gap: 10px; align-items: flex-start; margin-top: 14px;
}
.f-newsletter-row input[type="checkbox"] {
  width: 16px; height: 16px; margin-top: 2px; flex-shrink: 0;
  cursor: pointer; accent-color: var(--accent);
}
.f-newsletter-row label {
  font-size: .73rem; color: var(--gray); line-height: 1.5; cursor: pointer;
}
.f-newsletter-row label a { color: var(--accent-deep); text-decoration: underline; }

/* Thank You state */
.f-thankyou { display: none; text-align: center; padding: 12px 0; }
.f-thankyou.visible { display: block; }
.f-ty-icon { width: 44px; height: 44px; margin: 0 auto 12px; color: var(--accent); }
.f-ty-h { font-size: 1.25rem; font-weight: 800; letter-spacing: -.02em; margin-bottom: 8px; }
.f-ty-p { font-size: .93rem; color: var(--gray); line-height: 1.6; max-width: 320px; margin: 0 auto; }
.f-ty-p .hl { color: var(--accent-deep); font-weight: 600; }
.f-ty-divider { border: none; border-top: 1px solid var(--card-line); margin: 22px 0; }
.f-ty-profile {
  display: flex; align-items: center; gap: 14px;
  background: #f9fafb; border: 1px solid var(--card-line);
  border-radius: 12px; padding: 14px 16px; text-align: left; margin-bottom: 20px;
}
.f-ty-avatar {
  width: 52px; height: 52px; border-radius: 50%; flex-shrink: 0;
  background: var(--accent-soft); display: flex; align-items: center; justify-content: center;
  font-size: 1.4rem; overflow: hidden;
}
.f-ty-avatar img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
.f-ty-name { font-size: .88rem; font-weight: 700; color: var(--ink); }
.f-ty-role { font-size: .75rem; color: var(--gray); margin-bottom: 4px; }
.f-ty-quote { font-size: .8rem; color: var(--ink); line-height: 1.5; font-style: italic; }
.f-ty-cal-label { font-size: .8rem; color: var(--gray); text-align: center; margin-bottom: 12px; }
.f-ty-cal-label strong { display: block; font-size: .93rem; color: var(--ink); margin-top: 2px; }

/* ═══════════════════════════════════════════════
   MOCKUP SECTION — Referenz-Stil (Faris Schmidt)
   Exakt nachgebaut: Dot-Grid + Deep Shadow + Spring Float-Cards
═══════════════════════════════════════════════ */
.mockup-section { padding: 40px 0 96px; }

/* Äußerer Wrapper — position:relative für Float-Cards */
.mockup-wrap {
  position: relative;
  max-width: 860px; margin: 0 auto; padding: 0 24px;
}

/* Hauptkarte — identische Struktur wie Referenz:
   Dot-Grid + Verlauf + tiefer Schatten (0 40px 80px -20px) */
.mockup {
  background: linear-gradient(160deg, #eef4f0, #fff);
  background-image:
    radial-gradient(circle, rgba(34,197,94,.13) 1px, transparent 1.4px);
  background-size: 22px 22px;
  border: 1px solid var(--line);
  border-radius: 24px;
  padding: 24px;
  box-shadow: 0 40px 80px -20px rgba(13,15,26,.22);
}

/* Inner-Card — weißes, klares Fenster */
.mockup-inner {
  background: var(--white);
  border: 1px solid var(--line);
  border-radius: 16px;
  padding: 24px 28px;
  text-align: left;
}

/* Browser-Dots (Signatur-Detail aus Referenz) */
.mockup-bar { display: flex; gap: 6px; margin-bottom: 20px; }
.mockup-bar span { width: 10px; height: 10px; border-radius: 50%; background: var(--line); }

/* Kalender-Content innerhalb mockup-inner */
.cal-header {
  display: flex; align-items: center; justify-content: space-between; margin-bottom: 18px;
}
.cal-live {
  display: flex; align-items: center; gap: 7px;
  font-size: .72rem; font-weight: 700; text-transform: uppercase;
  letter-spacing: .04em; color: var(--accent-deep);
}
.cal-service { font-size: .82rem; font-weight: 700; color: var(--ink); }
.cal-service small { display: block; font-size: .7rem; font-weight: 400; color: var(--gray); }
.cal-avtr {
  width: 30px; height: 30px; border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--accent-deep));
  color: #fff; font-size: .72rem; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
}
.cal-split { display: grid; grid-template-columns: 1fr 1px 200px; gap: 0; }
@media (max-width: 680px) { .cal-split { grid-template-columns: 1fr; } }
.cal-divider { background: var(--line); margin: 0 20px; }
@media (max-width: 680px) { .cal-divider { display: none; } }

/* Kalender-Grid */
.cal-grid-wrap { padding-right: 20px; }
@media (max-width: 680px) { .cal-grid-wrap { padding-right: 0; margin-bottom: 18px; } }
.cal-month-row { display: flex; align-items: center; justify-content: space-between; margin-bottom: 12px; }
.cal-month { font-size: .85rem; font-weight: 700; color: var(--ink); }
.cal-navs { display: flex; gap: 5px; }
.cal-nav {
  width: 24px; height: 24px; border: 1px solid var(--card-line); border-radius: 5px;
  background: none; cursor: pointer; color: var(--gray); font-size: .78rem;
  display: flex; align-items: center; justify-content: center; transition: background .15s;
}
.cal-nav:hover { background: var(--accent-soft); color: var(--accent-deep); }
.cal-wdays { display: grid; grid-template-columns: repeat(7,1fr); text-align: center; margin-bottom: 5px; }
.cal-wdays span { font-size: .62rem; font-weight: 600; color: var(--gray-light); padding: 2px 0; }
.cal-days { display: grid; grid-template-columns: repeat(7,1fr); gap: 1px; text-align: center; }
.cal-days span { padding: 5px 0; font-size: .75rem; border-radius: 6px; color: var(--ink); }
.cal-days .av { color: var(--accent-deep); font-weight: 600; cursor: pointer; }
.cal-days .av:hover { background: var(--accent-soft); }
.cal-days .sel { background: var(--accent); color: #fff; font-weight: 700; }
.cal-days .td { font-weight: 700; border: 1.5px solid var(--accent); }
.cal-days .dim { color: #dde3dd; }

/* Zeitslots rechte Spalte */
.cal-times-col { padding-left: 20px; display: flex; flex-direction: column; justify-content: center; }
@media (max-width: 680px) { .cal-times-col { padding-left: 0; border-top: 1px solid var(--line); padding-top: 14px; } }
.cal-times-label { font-size: .7rem; font-weight: 700; color: var(--gray); margin-bottom: 10px; text-transform: uppercase; letter-spacing: .05em; }
.cal-slots { display: flex; flex-direction: column; gap: 7px; }
.cal-slot {
  padding: 8px 14px; border-radius: 8px; border: 1px solid var(--card-line);
  font-size: .8rem; background: none; cursor: pointer; color: var(--ink);
  text-align: center; transition: all .15s;
}
.cal-slot:hover { border-color: var(--accent); color: var(--accent-deep); }
.cal-slot.sel { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
.cal-confirm-btn {
  display: block; width: 100%; margin-top: 12px;
  background: var(--ink); color: #fff;
  font-family: var(--f-sans); font-size: .85rem; font-weight: 600;
  padding: 10px; border-radius: 8px; border: none; cursor: pointer;
  box-shadow: rgba(255,255,255,.10) 0 0 0 2px inset;
  transition: background .2s, transform .2s;
}
.cal-confirm-btn:hover { background: #1a1a1a; transform: translateY(-1px); }

/* Float-Cards — exakt nach Referenz: Spring-Easing, tiefer Schatten */
.float-card {
  position: absolute;
  background: var(--white);
  border: 1px solid var(--line);
  border-radius: 16px;
  box-shadow: 0 20px 45px rgba(13,15,26,.14);
  padding: 16px 20px;
  opacity: 0; transform: translateY(16px) scale(.9);
  transition: opacity .5s cubic-bezier(.34,1.56,.64,1),
              transform .5s cubic-bezier(.34,1.56,.64,1);
  white-space: nowrap;
}
.mockup-wrap.is-visible .float-card { opacity: 1; transform: none; }

/* Float-Card: Statistik (links oben) */
.float-card.stat { top: 4%; left: -3%; transition-delay: .15s; }
.float-card.stat .num {
  font-family: var(--f-serif); font-size: 1.55rem; font-weight: 500;
  font-style: italic; color: var(--accent); line-height: 1.1; display: block;
}
.float-card.stat .lbl { font-size: .75rem; color: var(--gray); margin-top: 2px; display: block; }

/* Float-Card: Notification (rechts unten) */
.float-card.notif {
  bottom: 6%; right: -3%;
  display: flex; align-items: center; gap: 12px; max-width: 260px;
  transition-delay: .35s;
}
.float-card.notif .ic {
  width: 36px; height: 36px; border-radius: 50%;
  background: var(--accent-soft);
  display: flex; align-items: center; justify-content: center; flex: none;
}
.float-card.notif b { font-size: .84rem; display: block; color: var(--ink); }
.float-card.notif p { font-size: .74rem; color: var(--gray); margin: 0; }
@media (max-width: 700px) { .float-card { display: none; } }
@media (prefers-reduced-motion: reduce) { .float-card { opacity: 1; transform: none; transition: none; } }

/* ═══════════════════════════════════════════════
   PAIN SECTION
═══════════════════════════════════════════════ */
.section { padding: 80px 0; }
.section-head { text-align: center; max-width: 600px; margin: 0 auto 52px; }
.section-head h2 {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(1.7rem, 3.5vw, 2.4rem);
  letter-spacing: -.025em; line-height: 1.15; margin-bottom: 12px;
}
.section-head h2 em {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--accent);
}
.section-head p { color: var(--gray); font-size: 1rem; line-height: 1.65; }

.cards3 { display: grid; grid-template-columns: repeat(3,1fr); gap: 20px; }
@media (max-width: 800px) { .cards3 { grid-template-columns: 1fr; } }

/* Mockup-Karten — kein Schatten, nur Border wie Referenz */
.card-m {
  background: var(--white);
  border: 1px solid var(--card-line);
  border-radius: var(--radius-card);
  overflow: hidden;
}
.card-m .c-icon-bg {
  height: 160px; padding: 24px;
  background: linear-gradient(180deg, #f4f8f5 0%, #e8f2ea 45%, #ffffff 100%);
  display: flex; align-items: center; justify-content: center;
}
.card-m .c-body { padding: 22px 24px 26px; }
.card-m h3 {
  font-family: var(--f-serif); font-weight: 400; font-size: 1.2rem;
  letter-spacing: -.01em; color: var(--ink); margin-bottom: 8px;
}
.card-m p { font-size: .92rem; line-height: 1.6; color: var(--gray); }

/* SVG animation — same as reference */
.mi { opacity: 0; transform: translateY(6px); transform-box: fill-box; transform-origin: center; }
.card-m.reveal.is-visible .mi { animation: mi-in .6s cubic-bezier(.16,1,.3,1) forwards; }
.card-m.reveal.is-visible .mi.d1 { animation-delay: .1s; }
.card-m.reveal.is-visible .mi.d2 { animation-delay: .25s; }
.card-m.reveal.is-visible .mi.d3 { animation-delay: .4s; }
.card-m.reveal.is-visible .mi.d4 { animation-delay: .55s; }
@keyframes mi-in { to { opacity: 1; transform: none; } }
.mi-breathe { transform-box: fill-box; transform-origin: center; }
.card-m.reveal.is-visible .mi-breathe {
  animation: mi-in .6s cubic-bezier(.16,1,.3,1) .4s forwards,
             mi-breathe 3.5s ease-in-out 1.2s infinite;
}
@keyframes mi-breathe { 0%,100%{transform:scale(1)} 50%{transform:scale(1.06)} }
@media (prefers-reduced-motion: reduce) {
  .mi { opacity: 1; transform: none; animation: none !important; }
  .mi-breathe { animation: none !important; }
}

/* ═══════════════════════════════════════════════
   SOLUTION — List-Style
═══════════════════════════════════════════════ */
.solution-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 18px; }
@media (max-width: 640px) { .solution-grid { grid-template-columns: 1fr; } }
.sol-item {
  display: flex; gap: 14px; align-items: flex-start;
  background: var(--white); border: 1px solid var(--card-line);
  border-radius: 12px; padding: 18px 20px;
}
.sol-check {
  width: 28px; height: 28px; border-radius: 50%;
  background: var(--accent-soft); flex-shrink: 0;
  display: flex; align-items: center; justify-content: center;
  color: var(--accent-deep); font-size: .85rem; font-weight: 800; margin-top: 1px;
}
.sol-item strong { font-size: .93rem; font-weight: 700; color: var(--ink); display: block; margin-bottom: 2px; }
.sol-item span { font-size: .82rem; color: var(--gray); line-height: 1.45; }

/* ═══════════════════════════════════════════════
   GUARANTEE — White card, green accent
═══════════════════════════════════════════════ */
.guarantee-wrap { max-width: 640px; margin: 0 auto; }
.guarantee-card {
  background: var(--white);
  border: 1.5px solid var(--accent-line);
  border-radius: 20px;
  padding: 48px 40px;
  text-align: center;
  position: relative; overflow: hidden;
  box-shadow: 0 4px 12px rgba(34,197,94,.07), 0 20px 56px rgba(34,197,94,.05);
}
.guarantee-card::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--accent-deep));
}
.guarantee-icon { width: 48px; height: 48px; margin: 0 auto 16px; color: var(--accent); }
.guarantee-card h2 {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(1.4rem, 3vw, 2rem);
  letter-spacing: -.025em; line-height: 1.15; margin-bottom: 12px;
}
.guarantee-card h2 em {
  font-family: var(--f-serif); font-style: italic;
  font-weight: 500; color: var(--accent);
}
.guarantee-card p { color: var(--gray); font-size: .97rem; line-height: 1.65; max-width: 44ch; margin: 0 auto 28px; }
.guarantee-card .btns { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }

/* ═══════════════════════════════════════════════
   FOOTER — grün mit Footer-Wordmark
═══════════════════════════════════════════════ */
.footer-transition {
  height: 72px;
  background: linear-gradient(to bottom, #ffffff 0%, #0b2b15 100%);
}
footer {
  background: #0b2b15;
  padding: 48px 0 32px;
  position: relative; overflow: hidden;
}
footer .foot-links {
  display: flex; justify-content: space-between; flex-wrap: wrap; gap: 16px;
  font-size: .82rem; color: rgba(255,255,255,.4);
  position: relative; z-index: 2;
}
footer .foot-links a { transition: color .2s; }
footer .foot-links a:hover { color: rgba(255,255,255,.7); }
.foot-wordmark {
  font-family: var(--f-sans); font-weight: 800;
  font-size: clamp(3.5rem, 12vw, 9rem);
  color: var(--accent); opacity: .06;
  text-align: center; line-height: 1; letter-spacing: -.03em;
  margin-top: 18px;
  user-select: none; pointer-events: none; position: relative; z-index: 1;
  aria-hidden: "true";
}

/* ═══════════════════════════════════════════════
   RESPONSIVE
═══════════════════════════════════════════════ */
@media (max-width: 520px) {
  .form-card { padding: 24px 20px; }
  .guarantee-card { padding: 36px 24px; }
  .hero h1 { font-size: clamp(2rem, 8vw, 3rem); }
}

  </style>
</Fragment>


<!-- ─── URGENCY BAR ─────────────────────────────────────────── -->
<div class="urg-bar">
  Nur noch <strong>1 freier Platz</strong> in deiner Region — danach ist es zu spät
</div>


<!-- ─── NAV ────────────────────────────────────────────────── -->
<div class="nav-outer">
  <nav class="pill">
    <div class="nav-brand">
      <span class="nav-dot"></span>
      Fastra One
    </div>
    <ul>
      <li><a href="#problem">Problem</a></li>
      <li><a href="#loesung">Lösung</a></li>
      <li><a href="#garantie">Garantie</a></li>
    </ul>
    <button class="btn btn-primary btn-sm" onclick="scrollToForm()">
      Platz sichern
      <span class="btn-arrow">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
      </span>
    </button>
  </nav>
</div>


<!-- ─── HERO ────────────────────────────────────────────────── -->
<section class="hero">
  <div class="wrap">

    <span class="hero-badge reveal">
      <span class="pulse-dot"></span>
      Exklusiv für GaLaBau-Betriebe
    </span>

    <h1 class="reveal delay-1">
      <span class="serif-hook">"Ich bin der Beste in meiner Stadt —</span>
      aber kein Mensch <em>weiß das."</em>
    </h1>

    <p class="hero-sub reveal delay-2">
      Wir ändern das. In 30 Tagen.<br>
      <strong>3 garantierte Auftragsanfragen — oder du zahlst nichts.</strong>
    </p>

    <div class="guarantee-pill reveal delay-2">
      Oder du zahlst uns keinen Cent
    </div>

    <!-- Google Reviews -->
    <div class="google-row reveal delay-2">
      <span class="google-stars">★★★★★</span>
      <span class="google-text"><strong>4,9 / 5</strong> · Google-Bewertungen</span>
    </div>

    <!-- FORM CARD -->
    <div class="form-card reveal delay-3" id="heroForm">

      <!-- FORM AREA -->
      <div id="fFormArea">
        <div class="form-progress" id="fProgress">
          <div class="form-progress-dot active" id="fdot-0"></div>
          <div class="form-progress-dot" id="fdot-1"></div>
          <div class="form-progress-dot" id="fdot-2"></div>
          <div class="form-progress-dot" id="fdot-3"></div>
        </div>

        <!-- Step 0 — Vorname -->
        <div class="f-step visible" id="fstep-0">
          <p class="f-question">Wie ist dein Vorname?</p>
          <input class="f-input" id="f-vorname" type="text" placeholder="z.B. Thomas" autocomplete="given-name" />
          <p class="f-err" id="ferr-0">Bitte gib deinen Vornamen ein.</p>
          <button class="f-next-btn" onclick="fNext(0)">Weiter →</button>
        </div>

        <!-- Step 1 — Nachname -->
        <div class="f-step" id="fstep-1">
          <p class="f-question">Und dein Nachname?</p>
          <input class="f-input" id="f-nachname" type="text" placeholder="z.B. Müller" autocomplete="family-name" />
          <p class="f-err" id="ferr-1">Bitte gib deinen Nachnamen ein.</p>
          <button class="f-next-btn" onclick="fNext(1)">Weiter →</button>
        </div>

        <!-- Step 2 — Handynummer -->
        <div class="f-step" id="fstep-2">
          <p class="f-question">Deine Handynummer?</p>
          <input class="f-input" id="f-phone" type="tel" placeholder="z.B. 0162 1234567" autocomplete="tel" />
          <p class="f-err" id="ferr-2">Bitte gib eine gültige Nummer ein.</p>
          <p class="f-hint">Wir rufen dich an — kein Spam.</p>
          <button class="f-next-btn" onclick="fNext(2)">Weiter →</button>
        </div>

        <!-- Step 3 — E-Mail -->
        <div class="f-step" id="fstep-3">
          <p class="f-question">Deine E-Mail-Adresse?</p>
          <input class="f-input" id="f-email" type="email" placeholder="z.B. thomas@musterbau.de" autocomplete="email" />
          <p class="f-err" id="ferr-3">Bitte gib eine gültige E-Mail-Adresse ein.</p>
          <div class="f-newsletter-row">
            <input type="checkbox" id="f-newsletter" />
            <label for="f-newsletter">
              Ja, ich möchte auch kostenlose Tipps zur Auftragsgewinnung per E-Mail erhalten.
              (Optional · <a href="/datenschutz" target="_blank">Datenschutz</a>)
            </label>
          </div>
          <button class="f-next-btn" id="fSubmitBtn" onclick="fSubmit()">
            Jetzt kostenlos Platz sichern →
          </button>
        </div>
      </div>

      <!-- THANK YOU -->
      <div class="f-thankyou" id="fThankyou">
        <div class="f-ty-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="44" height="44"><circle cx="12" cy="12" r="10"/><polyline points="7 12 10.5 15.5 17 9"/></svg>
        </div>
        <h2 class="f-ty-h">Du bist dabei — wir freuen uns!</h2>
        <p class="f-ty-p">
          Deine Anfrage ist angekommen. Wir melden uns
          <span class="hl">innerhalb der nächsten 24 Stunden</span>
          persönlich bei dir.
        </p>
        <hr class="f-ty-divider" />
        <div class="f-ty-profile">
          <div class="f-ty-avatar" id="paulAvatarBox">
            <!-- FOTO: Paul schickt sein Bild, dann wird der Emoji ersetzt durch <img src="..." alt="Paul Fasterding"> -->
            👨‍💼
          </div>
          <div>
            <div class="f-ty-role">Gründer · Fastra One</div>
            <div class="f-ty-name">Paul Fasterding</div>
            <p class="f-ty-quote">"Ich freue mich darauf — und zeige dir, wie das System wirklich funktioniert."</p>
          </div>
        </div>
        <p class="f-ty-cal-label">
          Du willst nicht warten?
          <strong>Buch direkt einen Termin mit Paul</strong>
        </p>
        <div
          class="calendly-inline-widget"
          data-url="https://calendly.com/paul-web/30min?hide_gdpr_banner=1&primary_color=22c55e"
          style="min-width:280px; height:640px;"
        ></div>
        <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
      </div>

    </div><!-- /form-card -->

    <div class="reveal delay-3" style="margin-top:16px; display:flex; justify-content:center; gap:20px; flex-wrap:wrap;">
      <span style="font-size:.76rem; color:var(--gray); display:flex; align-items:center; gap:5px;">
        <span style="color:var(--accent); font-weight:800;">✓</span> Kostenlos &amp; unverbindlich
      </span>
      <span style="font-size:.76rem; color:var(--gray); display:flex; align-items:center; gap:5px;">
        <span style="color:var(--accent); font-weight:800;">✓</span> Antwort in 24h
      </span>
      <span style="font-size:.76rem; color:var(--gray); display:flex; align-items:center; gap:5px;">
        <span style="color:var(--accent); font-weight:800;">✓</span> 1 Betrieb pro Region
      </span>
    </div>

  </div>
</section>


<!-- ─── MOCKUP SECTION (Referenz-Stil: Dot-Grid + Float-Cards) ─ -->
<section class="mockup-section">
  <div class="wrap">

    <div class="mockup-wrap reveal">
      <div class="mockup">
        <div class="mockup-inner">

          <!-- Browser-Dots -->
          <div class="mockup-bar">
            <span></span><span></span><span></span>
          </div>

          <!-- Header: Live-Badge + Service + Avatar -->
          <div class="cal-header">
            <div class="cal-live">
              <span class="pulse-dot"></span>
              Live Buchungen
            </div>
            <div class="cal-service">
              Termin buchen
              <small>GaLaBau-Beratung · 30 Min · Kostenlos</small>
            </div>
            <div class="cal-avtr">FO</div>
          </div>

          <!-- Split: Kalender-Grid | Zeitslots -->
          <div class="cal-split">

            <!-- Kalender -->
            <div class="cal-grid-wrap">
              <div class="cal-month-row">
                <span class="cal-month">August 2026</span>
                <div class="cal-navs">
                  <button class="cal-nav">‹</button>
                  <button class="cal-nav">›</button>
                </div>
              </div>
              <div class="cal-wdays">
                <span>Mo</span><span>Di</span><span>Mi</span>
                <span>Do</span><span>Fr</span><span>Sa</span><span>So</span>
              </div>
              <div class="cal-days">
                <span class="dim"></span><span class="dim"></span><span class="dim"></span>
                <span class="dim"></span><span class="av">1</span><span class="dim">2</span><span class="dim">3</span>
                <span class="av">4</span><span class="av">5</span><span class="av">6</span>
                <span class="av">7</span><span class="av">8</span><span class="dim">9</span><span class="dim">10</span>
                <span class="av">11</span><span class="av">12</span><span class="td">13</span>
                <span class="sel">14</span><span class="av">15</span><span class="dim">16</span><span class="dim">17</span>
                <span class="av">18</span><span class="av">19</span><span class="av">20</span>
                <span class="av">21</span><span class="av">22</span><span class="dim">23</span><span class="dim">24</span>
                <span class="av">25</span><span class="av">26</span><span class="av">27</span>
                <span class="av">28</span><span class="av">29</span><span class="dim">30</span><span class="dim">31</span>
              </div>
            </div>

            <!-- Divider -->
            <div class="cal-divider"></div>

            <!-- Zeitslots -->
            <div class="cal-times-col">
              <div class="cal-times-label">Do, 14. August</div>
              <div class="cal-slots">
                <button class="cal-slot">09:00 Uhr</button>
                <button class="cal-slot sel">10:30 Uhr</button>
                <button class="cal-slot">14:00 Uhr</button>
                <button class="cal-slot">15:30 Uhr</button>
              </div>
              <button class="cal-confirm-btn">Termin bestätigen →</button>
            </div>

          </div>

        </div>
      </div>

      <!-- Float-Card: Statistik (links oben) — Fraunces-Zahl wie Referenz -->
      <div class="float-card stat">
        <span class="num">+3.200 €</span>
        <span class="lbl">Ø Umsatz pro Monat<br>durch neue Kunden</span>
      </div>

      <!-- Float-Card: Notification (rechts unten) -->
      <div class="float-card notif">
        <div class="ic">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent-deep)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12"/>
          </svg>
        </div>
        <div>
          <b>Booking bestätigt</b>
          <p>Thomas M. · Mo, 14. Aug · 10:30 Uhr</p>
        </div>
      </div>

    </div>

  </div>
</section>


<!-- ─── PROBLEM ─────────────────────────────────────────────── -->
<section class="section" id="problem" style="padding-top:0; background:#f7f8fa;">
  <div class="wrap">
    <div class="section-head reveal">
      <h2>Warum dein Auftragsbuch jeden Monat <em>zittert</em></h2>
      <p>Drei Muster, die fast jeden GaLaBau-Betrieb bremsen.</p>
    </div>
    <div class="cards3">

      <div class="card-m reveal">
        <div class="c-icon-bg">
          <svg width="180" height="120" viewBox="0 0 180 120" fill="none">
            <g class="mi d1">
              <rect x="20" y="14" width="100" height="30" rx="12" fill="#EEF2EF"/>
              <rect x="34" y="24" width="60" height="6" rx="3" fill="#B7C4BB"/>
              <rect x="34" y="33" width="40" height="5" rx="2.5" fill="#D4E0D6"/>
            </g>
            <g class="mi d3">
              <rect x="55" y="66" width="100" height="30" rx="12" fill="#22c55e"/>
              <rect x="69" y="76" width="60" height="6" rx="3" fill="rgba(255,255,255,.8)"/>
              <rect x="69" y="85" width="42" height="5" rx="2.5" fill="rgba(255,255,255,.5)"/>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>"Wir leben von Mundpropaganda."</h3>
          <p>Klappt jahrelang. Bis es plötzlich nicht mehr klappt — und du nicht weißt, wann das nächste Telefon klingelt.</p>
        </div>
      </div>

      <div class="card-m reveal delay-1">
        <div class="c-icon-bg">
          <svg width="180" height="120" viewBox="0 0 180 120" fill="none">
            <g class="mi d1">
              <rect x="10" y="75" width="160" height="6" rx="3" fill="#E3E8E4"/>
            </g>
            <g class="mi d2">
              <rect x="22" y="52" width="20" height="29" rx="4" fill="#B7C4BB"/>
              <rect x="52" y="44" width="20" height="37" rx="4" fill="#B7C4BB"/>
              <rect x="82" y="56" width="20" height="25" rx="4" fill="#B7C4BB"/>
              <rect x="112" y="42" width="20" height="39" rx="4" fill="#B7C4BB"/>
            </g>
            <g class="mi d4">
              <rect x="142" y="20" width="20" height="61" rx="4" fill="#ef4444"/>
              <text x="152" y="14" text-anchor="middle" font-size="9" fill="#ef4444" font-family="Inter,sans-serif" font-weight="700">–40%</text>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>Oktober, November, Dezember: <em>Stille.</em></h3>
          <p>Mitarbeiter, Fixkosten, kein Eingang. Du zahlst aus der Substanz — und kämpfst um jeden Auftrag.</p>
        </div>
      </div>

      <div class="card-m reveal delay-2">
        <div class="c-icon-bg">
          <svg width="180" height="120" viewBox="0 0 180 120" fill="none">
            <g class="mi d1">
              <circle cx="90" cy="50" r="28" fill="#EEF2EF" stroke="#DCE4DF" stroke-width="1.5"/>
              <path d="M78 46h24M78 54h18M82 38h14" stroke="#B7C4BB" stroke-width="2" stroke-linecap="round"/>
            </g>
            <g class="mi d3 mi-breathe">
              <circle cx="152" cy="26" r="12" fill="#22c55e"/>
              <path d="M147 26l3 3 6-6" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </g>
            <g class="mi d2">
              <line x1="90" y1="78" x2="140" y2="30" stroke="#B7C4BB" stroke-width="1" stroke-dasharray="3 3"/>
            </g>
          </svg>
        </div>
        <div class="c-body">
          <h3>Dein Konkurrent <em>kassiert</em> deine Anfragen.</h3>
          <p>Er findet sich auf Google — du nicht. Nicht weil er besser ist. Sondern weil er sichtbar ist.</p>
        </div>
      </div>

    </div>
  </div>
</section>


<!-- ─── LÖSUNG ──────────────────────────────────────────────── -->
<section class="section" id="loesung">
  <div class="wrap">
    <div class="section-head reveal">
      <h2>Dein <em>Auftragssystem</em> — komplett.</h2>
      <p>Alles was du brauchst, um planbar Anfragen zu bekommen. Wir bauen es. Du nutzt es.</p>
    </div>
    <div class="solution-grid">
      <div class="sol-item reveal">
        <div class="sol-check">✓</div>
        <div>
          <strong>Webseite die Anfragen bringt</strong>
          <span>Speziell für GaLaBau-Betriebe — kein Standard-Template.</span>
        </div>
      </div>
      <div class="sol-item reveal delay-1">
        <div class="sol-check">✓</div>
        <div>
          <strong>Google Ads in deiner Region</strong>
          <span>Wir schalten und optimieren. Du bekommst die Anfragen.</span>
        </div>
      </div>
      <div class="sol-item reveal delay-1">
        <div class="sol-check">✓</div>
        <div>
          <strong>Live-Dashboard mit deinen Anfragen</strong>
          <span>Name, Nummer, Projekt — sofort auf dein Handy.</span>
        </div>
      </div>
      <div class="sol-item reveal delay-2">
        <div class="sol-check">✓</div>
        <div>
          <strong>Exklusivität in deiner Region</strong>
          <span>Nur 1 GaLaBauer pro Region. Dein Wettbewerber bleibt draußen.</span>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ─── GARANTIE ────────────────────────────────────────────── -->
<section class="section" id="garantie" style="padding-top:0;">
  <div class="wrap">
    <div class="guarantee-wrap">
      <div class="guarantee-card reveal">
        <div class="guarantee-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" width="48" height="48"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><polyline points="9 12 11.5 14.5 15 10"/></svg>
        </div>
        <h2>
          <em>3 Aufträge in 30 Tagen</em><br>
          — oder du zahlst nichts.
        </h2>
        <p>
          Wenn du nach 30 Tagen keine 3 qualifizierten Auftragsanfragen erhältst,
          zahlst du uns keinen Cent. Das Risiko liegt komplett bei uns — nicht bei dir.
        </p>
        <div class="btns">
          <button class="btn btn-primary" onclick="scrollToForm()">
            Jetzt kostenlos anfragen
            <span class="btn-arrow">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            </span>
          </button>
          <a href="https://calendly.com/paul-web/30min" target="_blank" rel="noopener" class="btn btn-outline">
            Direkt Termin buchen
          </a>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ─── FOOTER TRANSITION ────────────────────────────────────── -->
<div class="footer-transition"></div>


<!-- ─── FOOTER ──────────────────────────────────────────────── -->
<footer>
  <div class="wrap">
    <div class="foot-links">
      <span>© 2026 Fastra One · Paul Fasterding · Am Flachsgraben 9, 30657 Hannover</span>
      <div style="display:flex; gap:20px;">
        <a href="/datenschutz">Datenschutz</a>
        <a href="/impressum">Impressum</a>
      </div>
    </div>
  </div>
  <div class="foot-wordmark" aria-hidden="true">Fastra One</div>
</footer>


<script is:inline>

/* ─── Form State ─── */
var _fd = { vorname:'', nachname:'', phone:'', email:'', newsletter: false };
var _fstep = 0;

function scrollToForm() {
  document.getElementById('heroForm').scrollIntoView({ behavior:'smooth', block:'center' });
  setTimeout(function(){ document.getElementById('f-vorname').focus(); }, 500);
}

function fUpdateProgress(step) {
  for (var i = 0; i < 4; i++) {
    var d = document.getElementById('fdot-' + i);
    d.className = 'form-progress-dot';
    if (i < step)  d.classList.add('done');
    if (i === step) d.classList.add('active');
  }
}

function fShowStep(n) {
  for (var i = 0; i < 4; i++) {
    var s = document.getElementById('fstep-' + i);
    if (s) s.className = 'f-step' + (i === n ? ' visible' : '');
  }
  fUpdateProgress(n);
  _fstep = n;
  var ids = ['f-vorname','f-nachname','f-phone','f-email'];
  var inp = document.getElementById(ids[n]);
  if (inp) setTimeout(function(){ inp.focus(); }, 80);
}

function fNext(current) {
  var ids  = ['f-vorname','f-nachname','f-phone','f-email'];
  var keys = ['vorname','nachname','phone','email'];
  var val  = document.getElementById(ids[current]).value.trim();
  var errEl = document.getElementById('ferr-' + current);
  var ok = false;
  if (current === 0) ok = val.length >= 2;
  if (current === 1) ok = val.length >= 2;
  if (current === 2) ok = val.replace(/\s/g,'').length >= 6;
  if (current === 3) ok = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val);
  if (!ok) {
    errEl.className = 'f-err show';
    document.getElementById(ids[current]).classList.add('error');
    return;
  }
  errEl.className = 'f-err';
  document.getElementById(ids[current]).classList.remove('error');
  _fd[keys[current]] = val;
  fShowStep(current + 1);
}

function fSubmit() {
  var email = document.getElementById('f-email').value.trim();
  var errEl = document.getElementById('ferr-3');
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
    errEl.className = 'f-err show';
    document.getElementById('f-email').classList.add('error');
    return;
  }
  _fd.email = email;
  _fd.newsletter = document.getElementById('f-newsletter').checked;

  var btn = document.getElementById('fSubmitBtn');
  btn.disabled = true;
  btn.textContent = 'Wird gesendet…';

  var params = new URLSearchParams(window.location.search);
  var payload = {
    name: _fd.vorname + ' ' + _fd.nachname,
    vorname:   _fd.vorname,
    nachname:  _fd.nachname,
    phone:     _fd.phone,
    email:     _fd.email,
    newsletter: _fd.newsletter,
    seite:     'galabau-garantie',
    utm_source:    params.get('utm_source')   || '',
    utm_medium:    params.get('utm_medium')   || '',
    utm_campaign:  params.get('utm_campaign') || '',
    utm_content:   params.get('utm_content')  || '',
    created_at: new Date().toISOString()
  };

  fetch('https://fastraone-admin.vercel.app/api/leads', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload)
  })
  .then(function(r) { return r.ok ? r : Promise.reject(r.status); })
  .then(function() {
    if (window.dataLayer) window.dataLayer.push({ event:'lead_submit', lead_seite:'galabau-garantie' });
    fShowThankyou();
  })
  .catch(function() { fShowThankyou(); });
}

function fShowThankyou() {
  document.getElementById('fFormArea').style.display = 'none';
  document.getElementById('fThankyou').className = 'f-thankyou visible';
}

/* Enter-Taste */
document.addEventListener('keydown', function(e) {
  if (e.key !== 'Enter') return;
  var actions = [
    function(){ fNext(0); },
    function(){ fNext(1); },
    function(){ fNext(2); },
    function(){ fSubmit(); }
  ];
  if (actions[_fstep]) actions[_fstep]();
});

/* ─── Scroll Reveal ─── */
(function(){
  var els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) {
    els.forEach(function(e){ e.classList.add('is-visible'); });
    return;
  }
  var io = new IntersectionObserver(function(entries){
    entries.forEach(function(entry){
      if (entry.isIntersecting){
        entry.target.classList.add('is-visible');
        io.unobserve(entry.target);
      }
    });
  }, { threshold: 0.12 });
  els.forEach(function(e){ io.observe(e); });
})();

</script>

</BaseLayout>
